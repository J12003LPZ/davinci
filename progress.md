# Rust rewrite progress

**Complete: 99%**


TypeScript spec: `vendor/pi` @ `853a80d26c90a14c1886f0ebb8ffaae133ca2185` (`@earendil-works/pi-*` 0.84.4).
Toolchain: Rust **1.83.0**, no `edition2024`, no `unsafe`. TypeScript remains in `vendor/pi` as the behavioral reference only. Rust under `crates/*` is the product.

A TypeScript `pi` user can install Rust `pi` and use the same flags, `~/.pi` sessions, provider credentials, TUI, print, RPC, JS extension registration (including CLI flags, keyboard shortcuts, and `sendMessage` turn/queue options), HTML export, package install/list/remove/update (local + npm/git trees), and `pi config` resource enable/disable.

## What landed

- Vendored TypeScript `pi` (reference only).
- Workspace crates mapping every `vendor/pi/packages/*` plus session-backends and Rust-only `pi-parity`: `pi-telemetry`, `pi-protocol`, `pi-session`, `pi-session-sqlite`, `pi-ai`, `pi-agent`, `pi-tui`, `pi-client`, `pi-server`, `pi-evals`, `pi-coding-agent` (`pi` binary), `pi-parity`. Each crate has TypeScript-fixture tests.
- **pi-ai**: 39 provider catalogs, env-key map, `auth.json`, OAuth authorize URLs + token fixture parse + refresh, Anthropic PKCE + `127.0.0.1:53692/callback` server (TypeScript client id, scopes, success/error HTML titles), `parseAuthorizationInput` (`url` / `code#state` / `code=`), RFC 8628 device-code polling (`Login cancelled` / `Device flow timed out` / WSL slow_down timeout, default 5s interval, `slow_down` +5s or server `interval`, `waitBeforeFirstPoll`), device login for GitHub Copilot / xAI / Kimi Code / OpenAI Codex / Radius (`PI_OAUTH_DEVICE_FIXTURE` / `PI_OAUTH_DEVICE_POLL_FIXTURE` / `PI_OAUTH_TOKEN_FIXTURE`, `PI_OAUTH_DEVICE_SLEEP_MS=0` so tests never sleep), OpenRouter OAuth (`callback_url` + PKCE, `POST /api/v1/auth/keys` storing `{key}` with TypeScript missing-key string), Codex browser callback `localhost:1455/auth/callback` (`id_token_add_organizations`, `originator=pi`), Radius callback `127.0.0.1:1456/oauth/callback`, Radius browser `GET {gateway}/v1/oauth` discovery (`authorizationEndpoint`, TypeScript invalid/HTTP error strings, `PI_RADIUS_OAUTH_DISCOVERY_FIXTURE`), request bodies for anthropic-messages / openai-completions / openai-responses / google-generative-ai / google-vertex / pi-messages / openrouter-images / bedrock-converse-stream / mistral-conversations, SSE corpora, usage/cost, retry classification, live `ureq` SSE when credentials + `allow_network` (tests stay fixture-only; `PI_DISABLE_NETWORK=1` blocks live calls). Error string `No API key for provider: {id}`. Bedrock SigV4 (`AWS_ACCESS_KEY_ID` / secret / session token) and `AWS_BEARER_TOKEN_BEDROCK`. Vertex ADC marker `gcp-vertex-credentials`, placeholder `/^<[^>]+>$/`, `GOOGLE_CLOUD_PROJECT` / `GCLOUD_PROJECT` / `GOOGLE_CLOUD_LOCATION` with TypeScript error strings, fixture ADC `token` / `access_token`. Codex transports `sse|websocket|websocket-cached|auto` with account-scoped cache and SSE fallback when a websocket does not open.
- **pi-session / sqlite**: JSONL v4 header, v3→v4 migrate, continue/resume/fork/clone/fork-from-entry, entries/tree/stats/leaf, `~/.pi` + `--session-dir`. Session tree `SessionRepo` (memory + SQLite) runs TypeScript `conformance.ts`. SQLite applies TypeScript `001_initial.sql` plus sequences, stats, branch tips/entries, lanes, records, facts, FTS5, fence leases, TS lease timing errors.
- **pi-agent**: compaction, skills, templates, context files (`AGENTS.override.md` then `AGENTS.md` / `AGENTS.MD` / `CLAUDE.md` / `CLAUDE.MD`, ancestor walk, `{agentDir}` layer, nested git-worktree shadowing of the main-repo context file), builtin tools read/write/edit/bash/ls/grep/find, Ask/Allow/Deny permission policies, retry, steer/follow-up queue modes, thinking cycle, fixture-driven loop that executes tool calls. Codex transport + session id are forwarded on `openai-codex`. `--system-prompt` / `--append-system-prompt` resolve as file paths (BOM-stripped) like TypeScript `resolvePromptInput`.
- **pi-tui**: `Component::render(width)`, wrap, `wrapTextWithAnsi`, editor, keybindings + Ctrl/CSI byte decode, markdown, mouse/alt-buffer, raw `stty` input helpers, themes, selectors, Container/Overlay/ChatView, differential `DiffScreen`, SGR 1006 mouse + overlay hit-test, Box/VStack/HStack/ScrollView/Input/SettingsList, focused Input IME cursor (`CURSOR_MARKER` `\x1b_pi:c\x07` + reverse-video cell), `extractCursorPosition` (viewport scan, strip marker, `visibleWidth` column) + CUP hardware cursor, `applyLineResets` / `normalizeTerminalOutput` (Thai/Lao AM, tab→3 spaces, skip image lines), Kitty/iTerm2 protocol (`encodeKitty` / `encodeITerm2` / delete / crop / placement / `renderImage` / capability detect from TERM/PI_* fixtures), bracketed paste (strip CR/LF, tab→4 spaces), Ctrl+W/U/K kill-ring with accumulation, Ctrl+Y yank, Alt+Y yank-pop, Ctrl+- undo with type-word coalescing, `findWordBackward`/`findWordForward` (ASCII punctuation + CJK pairs), OSC 0 `setTitle`.
- **pi-protocol/client/server**: Unix+TCP+memory, handshake timeout, leases, request correlation, CBOR vectors. Server implements the full TypeScript `Command` union (list/create/attach/detach/prompt/steer/abort/set_model/set_thinking).
- **pi-evals / pi-parity**: fixture evals + required golden corpora. Optional `--parallel-run` / `--diff-jsonl`.
- **pi binary**: flags/subcommands from `args.ts` / `main.ts`, print/json/rpc/interactive sharing `SessionRuntime`. RPC `{type:"response",command,success}` plus streamed agent events. Prompt `streamingBehavior` queues steer/follow-up while a turn is running. Unknown command error `Unknown command: {type}`. Extension UI host implements every `RpcExtensionUIRequest` method (`select`, `confirm`, `input`, `editor`, `notify`, `setStatus`, `setWidget`, `setTitle`, `set_editor_text`) with pending-response correlation matching TypeScript. Interactive applies those requests to the TUI (title, footer statuses, widgets, selectors, editor text). JS extensions receive `pi.on` plus `ctx.ui` / `hasUI` in the Node host; `session_start` / `turn_start` / `turn_end` apply collected UI calls. Interactive raw-stdin key loop and builtin slash handlers. `/trust` opens the TypeScript `TrustSelectorComponent` chrome (`Project trust`, saved decision including inherited paths, current session, `→`/`✓`, `↑↓ navigate  enter save  escape cancel`) and writes `trust.json` on confirm. `/login` lists `Sign in with an account` / `Sign in with an API key` (xAI `Sign in with SuperGrok or X Premium`, Kimi `Sign in with Kimi Code`), then providers. Interactive `/login` replaces the editor with TypeScript `LoginDialogComponent` chrome (`Login to {name}`, OSC-8 URL, `Enter code: {userCode}`, `Waiting for authentication...`, focused Input, `escape to cancel`). API keys come from the Input, `PI_LOGIN_API_KEY`, or a value. OAuth uses the callback server + `PI_OAUTH_CALLBACK_URL` / `PI_OAUTH_CODE` / `PI_OAUTH_TOKEN_FIXTURE`, or device-code polling for GitHub Copilot / xAI / Kimi Code / Codex / Radius. Success strings: `Logged in to {name}. Credentials saved to {auth.json}` / `Saved API key for {name}. Credentials saved to {path}`. `/logout` only removes stored credentials and uses the TypeScript empty-store string. `openBrowser` matches TypeScript (`open` / `rundll32 url.dll,FileProtocolHandler` / `xdg-open`, no shell). `/copy` uses platform clipboard tools then OSC 52 with TypeScript error strings. `/changelog` parses `CHANGELOG.md` version headers and normalizes GitHub links. `/share` tries Radius (`https://radius.pi.dev/v1/artifacts`) when a bearer token is present, then a secret gist (`gh gist create --public=false`) with `PI_SHARE_VIEWER_URL` (`https://pi.dev/session/#<id>`).
- **JS extension API**: `pi.registerProvider` / `unregisterProvider` (name+config or native `{id}` object), `pi.registerTool` (schema + `execute(toolCallId, params, signal, onUpdate, ctx)` + optional `renderCall`/`renderResult`), `pi.registerCommand` (slash handler), `registerShortcut` dispatched from the interactive key loop (reserved `ctrl+c`/`ctrl+p`/`ctrl+t`/`escape`/`enter`/`ctrl+d`/`ctrl+z` cannot be overridden), `registerFlag` parsed as CLI flags with TypeScript errors (`Extension flag "--{name}" requires a value`, `Unknown option: --x` / `Unknown options: --a, --b`) and `getFlag`. `registerMessageRenderer` / `registerEntryRenderer` / `registerMarkdownTransformer` are captured and invoked (custom transcript lines, chained Markdown transforms on user/assistant text). `appendEntry` writes `type: "custom"`; `sendMessage` writes TypeScript `custom_message` entries and honors `triggerTurn` / `deliverAs` (`steer` / `followUp` / `nextTurn`, plus deferred append while streaming). `sendUserMessage` prompts or queues like TypeScript `prompt(..., { streamingBehavior })`. Load-time capture; tools join `ToolRegistry`; commands appear in `/help`, RPC `get_commands` (`source: "extension"`), print/RPC prompt, and interactive slash; providers merge into model list and `AgentConfig` (`baseUrl`, `api`, interpolated `apiKey`, headers, `authHeader`). `resolveConfigValue` matches `$ENV` / `${ENV}` / `$$` / `$!` / leading `!command`. Exact errors: `Provider config is required when registering by name`, `Provider id must not be empty.`
- **HTML export**: embeds TypeScript `template.html` / `template.css` / `template.js` plus vendored `marked.min.js` and `highlight.min.js`. Session JSONL is normalized to `{header, entries, leafId}` (message entries wrap `role`/`content`/`toolCallId`/`toolName`/`details`/`isError` as `message` when missing). Theme CSS vars come from `getResolvedThemeColors` / `getThemeExportColors` (builtin dark/light plus `PI_CODING_AGENT_DIR/themes/{name}.json`). `/export` and RPC `export_html` attach `systemPrompt`, tool schemas, and `renderedTools`. Custom tools (not `bash`/`read`/`write`/`edit`/`ls`) are pre-rendered via TUI→ANSI→HTML (`ansi-to-html.ts` SGR, whitespace fixture, grep/find formatters, JS `renderCall`/`renderResult`). `escapeHtml` / `sanitizeMarkdownUrl` stay as Rust helpers locked to the TypeScript allow-list.
- **Package manager**: `parseSource` matches TypeScript (`npm:`, `git:`, protocol URLs, local paths). Install writes TypeScript managed trees: `{agentDir}/npm/node_modules/{name}` and `{agentDir}/git/{host}/{path}` (project scope under `.pi/`). `PI_PACKAGE_FIXTURE` / `PI_PACKAGE_FIXTURE_MAP` copy local trees; `PI_DISABLE_NETWORK=1` blocks live `npm`/`git` spawns. `ensureNpmProject` writes `{name:"pi-extensions",private:true}` and `.gitignore`. `pi list` shows user packages always and project packages only when trusted (`--approve` / `--no-approve`, `trust.json` nearest-path store, `defaultProjectTrust: "always"` / `"never"` / `"ask"`, a user/global `project_trust` JS handler returning `yes`/`no` with optional `remember`, or the TypeScript project-trust selector). On TTY `ask`, stderr prints `Trust project folder?` plus Trust / Trust parent / session-only / Do not trust options and reads `Select:`; tests and non-interactive hosts use `PI_PROJECT_TRUST_SELECT` (label or 1-based index). Session-only decisions stay in-process. `/trust` shows the same options (without session-only) and `/trust <option>` writes `trust.json` with `Saved trust decision: trusted|untrusted. Restart pi for this to take effect.` Project `.pi` extensions and skills load only when trusted. Object sources are `(filtered)`. `pi update` uses saved trust only (plus approve override) and defaults to self (with the TypeScript extensions-skipped note); `--extensions` / `--extension` / a positional source refresh configured trees; `--models` refreshes catalogs. Managed installs (`PI_MANAGED_INSTALL_ROOT` + `managed-install.json` `releases-v1` + `PI_PACKAGE_DIR` inside `releases/`) activate existing release trees, refuse `--force`, take `update.lock`, and print `Updated pi from {VERSION} to {version}` / `pi is already up to date (v{VERSION})` / `Another managed Pi update is already running.` / invalid-marker errors. When `releases/{version}` is missing, Rust fetches `{PI_INSTALLER_API_BASE||https://pi.dev/api/installer/releases}/{version}/package.json` and `package-lock.json` (or `PI_INSTALLER_FIXTURE`), then runs TypeScript `npm ci --ignore-scripts --min-release-age=0 --omit=dev --include=optional --no-fund --no-audit --loglevel=error --progress=false`, verifies `{stage}/node_modules/.bin/pi --version`, and activates `current-version`. Download errors use `Could not download managed installer {label} from {url}: HTTP {status}`; `npm ci` failures use `npm {args} exited with code {code}`. Latest version comes from `PI_SELF_UPDATE_FIXTURE` / `PI_LATEST_VERSION_FIXTURE` or `https://pi.dev/api/latest-version` (`PI_OFFLINE` / `PI_DISABLE_NETWORK` fail with the TypeScript determine-latest-version error). Global npm/pnpm/yarn/bun installs that pass `getSelfUpdateCommand` (inferred `lib/node_modules` or `npm root -g`, writable package dir) spawn TypeScript argv (`install -g --ignore-scripts --min-release-age=0 {spec}`, optional `--prefix`) and print `Updating pi with {display}...` then `Updated pi from {VERSION} to {version}`; failures include the TypeScript fallback (`If this keeps failing, run this command yourself`) and the pnpm store-prune hint. Cargo/source installs print `error: pi cannot self-update this installation.` plus the TypeScript unknown-method instruction. Missing source / unknown option / `-l` on list match TypeScript CLI strings. Enabled package extensions join `discover_extensions`. `npmCommand` argv (including `mise … -- bun|pnpm|npm`) selects TypeScript install flags: bun `--cwd`/`--omit=peer`, pnpm `--prefix` plus peer-config flags, npm `--legacy-peer-deps`; git package `package.json` deps use `install --omit=dev` unless `npmCommand` is set (`install` only).
- **Config TUI**: `pi config` resolves package + auto-discovered extensions/skills/prompts/themes, renders TypeScript group/subgroup labels (`Global Resources`, `npm:… (user)`), search `Input` (`Search resources`), page up/down, and theme-colored `[x]`/`[ ]` plus project `[+]`/`[-]` with `project load` / `project unload` / `inherited global` suffixes. Space in project scope cycles inherit/+/- and persists `autoload: false` package overrides or top-level `+path`/`-path`. `-l` requires trust (`Project is not trusted. Use --approve to modify local resource config.`). Non-TTY prints the selector with DynamicBorder `─` chrome; TTY hosts it on `Tui` / `DiffScreen` with alt-buffer, raw keys, and ProcessTerminal-style borders.

- **System prompts**: `{cwd}/.pi/SYSTEM.md` and `APPEND_SYSTEM.md` load when the project is trusted; otherwise `{agentDir}/SYSTEM.md` and `APPEND_SYSTEM.md`. CLI `--system-prompt` / `--append-system-prompt` override discovery.

## What remains

- Interactive chat is not a line-for-line TypeScript `TuiAltScreen` image compositor: Kitty upload cache, reserved rows, eviction, and the `Image` component (square-box height, padding rows). Encode/placement/crop/capability detect match TypeScript fixtures; `CURSOR_MARKER` is extracted and hardware-positioned.
- Cargo/source installs print the TypeScript `unknown` self-update refusal; that is equivalent, not a missing product feature.

## Next crate/module

Port `TuiAltScreen` Kitty upload/eviction and the `Image` component, then re-audit the Done bar.

## Gates

```bash
cargo test --workspace && cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings
```

Last run: green on 1.83 after Input yank-pop, Radius OAuth discovery, CURSOR_MARKER hardware cursor, and Kitty/iTerm2 protocol helpers.
