# Debugging hard fixture: wrong-layer validation

The protocol constraint belongs at the shared boundary, not only in the UI
adapter. A repair that changes only presentation validation is insufficient.
