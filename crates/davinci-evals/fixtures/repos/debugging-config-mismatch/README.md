# Debugging hard fixture: configuration mismatch

The loader reads a different key than the documented configuration contract.
The repair must align the authoritative loader and schema, not edit unrelated
documentation or environment examples.
