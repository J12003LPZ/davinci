diff --git a/crates/davinci-coding-agent/src/native_extensions/graph/coordinator_handler.rs b/crates/davinci-coding-agent/src/native_extensions/graph/coordinator_handler.rs
index 1c2d0ad..b4987df 100644
--- a/crates/davinci-coding-agent/src/native_extensions/graph/coordinator_handler.rs
+++ b/crates/davinci-coding-agent/src/native_extensions/graph/coordinator_handler.rs
@@ -327,11 +327,13 @@ mod tests {
             if output.content.contains("RELATIVE_SCRIPT_OK") {
                 break;
             }
-            assert!(
-                Instant::now() < until,
-                "relative script failed: {}",
-                output.content
-            );
+            if Instant::now() >= until {
+                let status = worker.client().call("process_status", &json!({"id":id}));
+                panic!(
+                    "relative script failed: {}; process status: {status:?}",
+                    output.content
+                );
+            }
             std::thread::sleep(Duration::from_millis(10));
         }
     }
