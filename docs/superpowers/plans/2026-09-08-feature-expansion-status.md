# Feature expansion execution status

Full program remains incomplete. Status vocabulary: not started; in progress; implemented but not integrated; implemented but not verified; verified; blocked.

## Baseline and evidence

- Branch: `feature/expansion-20260908`.
- Worktree: `C:/Users/sergi/Desktop/pi-rust-feature-expansion`.
- Baseline source: `9c42820c1d4a61b3b0db43588667d492f0917b4e`.
- Original main checkout and unrelated modified CLAUDE.md/untracked plugins preserved. Expansion documents and current instructions copied into isolation; pre-existing CLAUDE.md changes excluded from task commits.
- All fourteen implementation plans and four companion documents read before application changes. F03 source specification consulted.
- Baseline command: `rtk cargo test -p davinci-agent --offline --locked runtime::tasks -- --nocapture`: exit 0; 10 passed, 355 filtered out. This proves only the existing task registry baseline.
- RTK used. Headroom returned a no-op because its configured local proxy was unreachable; no measured compression savings.
- Main session is sole writer; independent explorer audits existing task callers.

## Execution order and gates

Wave 0 establishes the baseline and contract ownership. Wave 1 establishes F03 metadata/CAS/journal and F01 approval. Wave 2 introduces control, budget, evidence and scope services through their designated owners. Wave 3 completes their host adapters, decisions, rewind and context. Wave 4 integrates semantic navigation, interaction backends and saved/controlled graphs. Wave 5 completes advanced graph operations and cross-feature acceptance.

Per-task dependencies below include prerequisite services, not blanket full-plan completion. All additional fixture scenarios in each linked task are mandatory. A task cannot become verified from its example helper alone.

## Implementation tasks

| Task | Deliverable | Dependencies / schedule | Status | Evidence / remaining integration |
|---|---|---|---|---|
| [F01-T1](2026-09-07-01-permission-approval-modal.md#task-1-policy-owned-legal-choices) | Policy-owned legal choices | W1 after F03 T1-3; preceding task interfaces where consumed | in progress | Policy-issued narrow scopes enforced by native/RPC and turn gate; offer_scopes and f01_legal_scope verified |
| [F01-T2](2026-09-07-01-permission-approval-modal.md#task-2-replay-resistant-challenge-resolution) | Replay-resistant challenge resolution | W1 after F03 T1-3; preceding task interfaces where consumed | in progress | Runtime PermissionRequested rejection stops callback/execution; typed challenges, reply_matches and f01_stale_reply verified |
| [F01-T3](2026-09-07-01-permission-approval-modal.md#task-3-modal-input-ownership-and-rendering) | Modal input ownership and rendering | W1 after F03 T1-3; preceding task interfaces where consumed | in progress | Modal overlay view, approval_key input trapping, numeric focus, explicit Enter and f01_modal_traps_mode_key verified |
| [F01-T4](2026-09-07-01-permission-approval-modal.md#task-4-durable-legal-scopes-and-session-lifecycle) | Durable legal scopes and session lifecycle | W1 after F03 T1-3; preceding task interfaces where consumed | in progress | Session-consent reset on load, project-save atomic retry, grant_applies and f01_grant_binding verified |
| [F01-T5](2026-09-07-01-permission-approval-modal.md#task-5-cross-host-parity-and-cancellation) | Cross-host parity and cancellation | W1 after F03 T1-3; preceding task interfaces where consumed | in progress | approval_host_result contract and noninteractive print/JSON fail-closed tested by f01_noninteractive_fail_closed; RPC EOF and cancellation verified |
| [F02-T1](2026-09-07-02-structured-clarification.md#task-1-typed-question-validation) | Typed question validation | W3 after F01; F03/F06 evidence integration; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F02-T2](2026-09-07-02-structured-clarification.md#task-2-authenticated-answer-transitions) | Authenticated answer transitions | W3 after F01; F03/F06 evidence integration; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F02-T3](2026-09-07-02-structured-clarification.md#task-3-tool-registration-and-bounded-waiting) | Tool registration and bounded waiting | W3 after F01; F03/F06 evidence integration; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F02-T4](2026-09-07-02-structured-clarification.md#task-4-decision-modal-and-rpc-adapters) | Decision modal and RPC adapters | W3 after F01; F03/F06 evidence integration; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F02-T5](2026-09-07-02-structured-clarification.md#task-5-plan-revisions-resume-and-task-prerequisites) | Plan revisions, resume, and task prerequisites | W3 after F01; F03/F06 evidence integration; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F03-T1](2026-09-07-03-execution-tasks-live-board.md#task-1-add-task-metadata-and-stable-presentation) | Add task metadata and stable presentation | W1 T1-3; W3 T4-5 after F02/F06; preceding task interfaces where consumed | in progress | Partial metadata checkpoint: additive defaults, four size limits, typed presentation and task-list adapter tested. Commit clock, checked revisions, atomic cascade overflow and completion revalidation now tested. Durable commit observations, replay revision reconstruction and lineage/plan resolution remain. |
| [F03-T2](2026-09-07-03-execution-tasks-live-board.md#task-2-owner-cas-and-explicit-handoff) | Owner CAS and explicit handoff | W1 T1-3; W3 T4-5 after F02/F06; preceding task interfaces where consumed | in progress | Atomic claims and guarded terminal updates now used by task_update; TeamManager claims/completion use the guarded registry. Runtime identity, revision and generation checks tested. Canonical idempotency, controller/creation policy, legacy mutation authority and fenced handoff remain outstanding. |
| [F03-T3](2026-09-07-03-execution-tasks-live-board.md#task-3-durable-task-commits-and-crash-recovery) | Durable task commits and crash recovery | W1 T1-3; W3 T4-5 after F02/F06; preceding task interfaces where consumed | in progress | Journal, operation receipts, host activation and fresh-process recovery tested on Windows. Host imports validated recordful legacy history through a versioned snapshot and persisted lineage. Pre-prompt/session-switch activation, parent-owned worker task transport, graph worker loopback connection and writer mutation authorization tested; record-less recovery, fresh-process migration and Unix validation remain outstanding. |
| [F03-T4](2026-09-07-03-execution-tasks-live-board.md#task-4-dependency-and-completion-transactions) | Dependency and completion transactions | W1 T1-3; W3 T4-5 after F02/F06; preceding task interfaces where consumed | in progress | Bounded run-scoped task_get and paginated task_list registered and read-lane tested. Full dependency retry evidence, F02 open-decision and F06 stale-evidence integration remain outstanding. |
| [F03-T5](2026-09-07-03-execution-tasks-live-board.md#task-5-live-task-board-plan-linkage-and-resume) | Live task board, plan linkage, and resume | W1 T1-3; W3 T4-5 after F02/F06; preceding task interfaces where consumed | in progress | Live board projection, TUI TaskBoard sheet, /tasks command, surface adapter with branch/root filtering, and all 8 required F03-T5 scenarios implemented and verified. Record-less recovery and cross-feature gates outstanding. |
| [F04-T1](2026-09-07-04-task-level-rewind.md#task-1-task-owned-checkpoints-and-byte-preserving-effects) | Task-owned checkpoints and byte-preserving effects | W3 after F03/F05/F06/F07 stop and drain; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F04-T2](2026-09-07-04-task-level-rewind.md#task-2-inverse-delta-planning-and-manual-edit-conflicts) | Inverse-delta planning and manual-edit conflicts | W3 after F03/F05/F06/F07 stop and drain; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F04-T3](2026-09-07-04-task-level-rewind.md#task-3-atomic-apply-stale-previews-and-recovery) | Atomic apply, stale previews, and recovery | W3 after F03/F05/F06/F07 stop and drain; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F04-T4](2026-09-07-04-task-level-rewind.md#task-4-tasktranscript-branch-restoration-and-evidence-invalidation) | Task/transcript branch restoration and evidence invalidation | W3 after F03/F05/F06/F07 stop and drain; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F04-T5](2026-09-07-04-task-level-rewind.md#task-5-irreversible-effects-and-confirmation-ui) | Irreversible effects and confirmation UI | W3 after F03/F05/F06/F07 stop and drain; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F05-T1](2026-09-07-05-task-scoped-contracts.md#task-1-contract-schema-and-accepted-plan-compilation) | Contract schema and accepted-plan compilation | W2 T1-3 after F03; W3 T4-5 after F01/F02/F06; preceding task interfaces where consumed | verified | TaskContract schema, deterministic SHA-256 canonical digest, path_scope_allows (protected strictly wins), path normalization, symlink escape checks, case sensitivity handling, semantic manifest/lockfile dependency verification, plan acceptance contract snapshot, and TaskRecord contract_digest integrated and verified |
| [F05-T2](2026-09-07-05-task-scoped-contracts.md#task-2-final-execution-gate-and-adapter-parity) | Final execution gate and adapter parity | W2 T1-3 after F03; W3 T4-5 after F01/F02/F06; preceding task interfaces where consumed | verified | contract_gate enforcement, DeclaredEffect, PreparedAction, ScopeViolation, member-level batch enforcement with stable IDs, dispatch-time recheck, child worker contract propagation, and all 7 required scenarios (alias bypass, batch safe first/forbidden second, transformed extension args, child loses scope env, unknown MCP mutation, owner handed off after preparation, poisoned mutex guard) verified |
| [F05-T3](2026-09-07-05-task-scoped-contracts.md#task-3-provable-process-and-external-effect-boundaries) | Provable process and external-effect boundaries | W2 T1-3 after F03; W3 T4-5 after F01/F02/F06; preceding task interfaces where consumed | verified | effect_profile_allows, fail-closed ContractExecutor capability boundary, canonical artifact-root validation, shell substitution detection, parent-owned graph contract propagation, host/graph/worker/verification integration, and required scenarios verified. No production process/filesystem/network sandbox backend is claimed: contracted shell/process/network and graph verification refuse before spawn; hard-contracted native filesystem writes refuse before mutation because no race-safe filesystem containment backend exists; advisory MCP readOnlyHint and unknown custom/native effects cannot grant authority and refuse before dispatch. |
| [F05-T4](2026-09-07-05-task-scoped-contracts.md#task-4-explicit-scope-expansion-decisions) | Explicit scope expansion decisions | W2 T1-3 after F03; W3 T4-5 after F01/F02/F06; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F05-T5](2026-09-07-05-task-scoped-contracts.md#task-5-audit-migration-and-verification-integration) | Audit, migration, and verification integration | W2 T1-3 after F03; W3 T4-5 after F01/F02/F06; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F06-T1](2026-09-07-06-evidence-backed-completion.md#task-1-canonical-source-manifests-and-evidence-identity) | Canonical source manifests and evidence identity | W2 T1-3 after F03; W3 T4-5 after F09 reserves; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F06-T2](2026-09-07-06-evidence-backed-completion.md#task-2-trusted-execution-receipts-and-artifacts) | Trusted execution receipts and artifacts | W2 T1-3 after F03; W3 T4-5 after F09 reserves; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F06-T3](2026-09-07-06-evidence-backed-completion.md#task-3-deterministic-completion-dimensions-and-gates) | Deterministic completion dimensions and gates | W2 T1-3 after F03; W3 T4-5 after F09 reserves; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F06-T4](2026-09-07-06-evidence-backed-completion.md#task-4-budget-reservation-and-installedruntime-proof) | Budget reservation and installed/runtime proof | W2 T1-3 after F03; W3 T4-5 after F09 reserves; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F06-T5](2026-09-07-06-evidence-backed-completion.md#task-5-completion-report-stale-refresh-and-evidence-export) | Completion report, stale refresh, and evidence export | W2 T1-3 after F03; W3 T4-5 after F09 reserves; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F07-T1](2026-09-07-07-live-task-agent-controls.md#task-1-revisioned-control-snapshots-and-command-authority) | Revisioned control snapshots and command authority | W2 T1-3 after F03; W3 T4-5 after F04/F05/F09; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F07-T2](2026-09-07-07-live-task-agent-controls.md#task-2-steering-mailbox-receipts-and-safe-boundaries) | Steering mailbox receipts and safe boundaries | W2 T1-3 after F03; W3 T4-5 after F04/F05/F09; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F07-T3](2026-09-07-07-live-task-agent-controls.md#task-3-owned-process-tree-cancellation) | Owned process-tree cancellation | W2 T1-3 after F03; W3 T4-5 after F04/F05/F09; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F07-T4](2026-09-07-07-live-task-agent-controls.md#task-4-retry-diff-and-explicit-handoff) | Retry, diff, and explicit handoff | W2 T1-3 after F03; W3 T4-5 after F04/F05/F09; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F07-T5](2026-09-07-07-live-task-agent-controls.md#task-5-interactive-panel-and-noninteractive-controls) | Interactive panel and noninteractive controls | W2 T1-3 after F03; W3 T4-5 after F04/F05/F09; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F08-T1](2026-09-07-08-context-memory-inspector.md#task-1-provider-view-manifest-and-selection-reasons) | Provider-view manifest and selection reasons | W3 after F03/F06 metadata; final context and memory adapters; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F08-T2](2026-09-07-08-context-memory-inspector.md#task-2-mandatory-safe-pin-and-exclude-overlays) | Mandatory-safe pin and exclude overlays | W3 after F03/F06 metadata; final context and memory adapters; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F08-T3](2026-09-07-08-context-memory-inspector.md#task-3-provenance-corrections-and-tombstones) | Provenance, corrections, and tombstones | W3 after F03/F06 metadata; final context and memory adapters; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F08-T4](2026-09-07-08-context-memory-inspector.md#task-4-snapshot-isolation-refresh-and-governor-compatibility) | Snapshot isolation, refresh, and governor compatibility | W3 after F03/F06 metadata; final context and memory adapters; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F08-T5](2026-09-07-08-context-memory-inspector.md#task-5-inspector-view-and-user-correction-workflow) | Inspector view and user correction workflow | W3 after F03/F06 metadata; final context and memory adapters; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F09-T1](2026-09-07-09-whole-task-budgets-loop-detection.md#task-1-root-ledger-and-reserve-classes) | Root ledger and reserve classes | W2 T1-2 after F03; T3 after F06 receipts; W3 T4-5 after F01/F07; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F09-T2](2026-09-07-09-whole-task-budgets-loop-detection.md#task-2-actual-usage-reconciliation-and-retry-charging) | Actual usage reconciliation and retry charging | W2 T1-2 after F03; T3 after F06 receipts; W3 T4-5 after F01/F07; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F09-T3](2026-09-07-09-whole-task-budgets-loop-detection.md#task-3-progress-fingerprints-and-deterministic-loop-signals) | Progress fingerprints and deterministic loop signals | W2 T1-2 after F03; T3 after F06 receipts; W3 T4-5 after F01/F07; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F09-T4](2026-09-07-09-whole-task-budgets-loop-detection.md#task-4-hard-limits-safe-pause-and-authorized-choices) | Hard limits, safe pause, and authorized choices | W2 T1-2 after F03; T3 after F06 receipts; W3 T4-5 after F01/F07; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F09-T5](2026-09-07-09-whole-task-budgets-loop-detection.md#task-5-budget-visibility-subsystem-adapters-and-regression-metrics) | Budget visibility, subsystem adapters, and regression metrics | W2 T1-2 after F03; T3 after F06 receipts; W3 T4-5 after F01/F07; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F10-T1](2026-09-07-10-semantic-code-navigation.md#task-1-service-contract-capabilities-and-fallback) | Service contract, capabilities, and fallback | W4 after F05/F06; transport and position tests may start W1; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F10-T2](2026-09-07-10-semantic-code-navigation.md#task-2-bounded-json-rpc-framing-and-lifecycle) | Bounded JSON-RPC framing and lifecycle | W4 after F05/F06; transport and position tests may start W1; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F10-T3](2026-09-07-10-semantic-code-navigation.md#task-3-document-versions-and-correct-position-conversion) | Document versions and correct position conversion | W4 after F05/F06; transport and position tests may start W1; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F10-T4](2026-09-07-10-semantic-code-navigation.md#task-4-trusted-server-launch-and-task-resource-integration) | Trusted server launch and task resource integration | W4 after F05/F06; transport and position tests may start W1; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F10-T5](2026-09-07-10-semantic-code-navigation.md#task-5-rename-preview-and-guarded-transactional-apply) | Rename preview and guarded transactional apply | W4 after F05/F06; transport and position tests may start W1; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F11-T1](2026-09-07-11-browser-terminal-interaction-testing.md#task-1-backend-interfaces-and-dependency-admission) | Backend interfaces and dependency admission | W4 T1-5 after F01/F05/F06/F07; W5 T6 cross-feature acceptance; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F11-T2](2026-09-07-11-browser-terminal-interaction-testing.md#task-2-owned-pty-lifecycle-and-terminal-screen-state) | Owned PTY lifecycle and terminal screen state | W4 T1-5 after F01/F05/F06/F07; W5 T6 cross-feature acceptance; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F11-T3](2026-09-07-11-browser-terminal-interaction-testing.md#task-3-deterministic-scenario-runner-and-assertions) | Deterministic scenario runner and assertions | W4 T1-5 after F01/F05/F06/F07; W5 T6 cross-feature acceptance; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F11-T4](2026-09-07-11-browser-terminal-interaction-testing.md#task-4-five-mode-cycle-and-modal-regression-suite) | Five-mode cycle and modal regression suite | W4 T1-5 after F01/F05/F06/F07; W5 T6 cross-feature acceptance; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F11-T5](2026-09-07-11-browser-terminal-interaction-testing.md#task-5-managed-browser-adapter-and-offline-fixture-network) | Managed browser adapter and offline fixture network | W4 T1-5 after F01/F05/F06/F07; W5 T6 cross-feature acceptance; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F11-T6](2026-09-07-11-browser-terminal-interaction-testing.md#task-6-evidence-integration-artifact-limits-and-release-matrix) | Evidence integration, artifact limits, and release matrix | W4 T1-5 after F01/F05/F06/F07; W5 T6 cross-feature acceptance; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F12-T1](2026-09-07-12-graph-save-reuse.md#task-1-strict-yaml-parser-and-safe-identifiers) | Strict YAML parser and safe identifiers | W4 after F03/F05/F06/F09; precedes F13; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F12-T2](2026-09-07-12-graph-save-reuse.md#task-2-definition-coherence-and-mandatory-native-gates) | Definition coherence and mandatory native gates | W4 after F03/F05/F06/F09; precedes F13; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F12-T3](2026-09-07-12-graph-save-reuse.md#task-3-compile-saved-topology-into-actual-native-execution) | Compile saved topology into actual native execution | W4 after F03/F05/F06/F09; precedes F13; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F12-T4](2026-09-07-12-graph-save-reuse.md#task-4-atomic-saveload-and-immutable-run-snapshots) | Atomic save/load and immutable run snapshots | W4 after F03/F05/F06/F09; precedes F13; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F12-T5](2026-09-07-12-graph-save-reuse.md#task-5-command-routing-and-safe-parameter-binding) | Command routing and safe parameter binding | W4 after F03/F05/F06/F09; precedes F13; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F12-T6](2026-09-07-12-graph-save-reuse.md#task-6-trust-replay-provenance-and-full-saved-run-acceptance) | Trust, replay provenance, and full saved-run acceptance | W4 after F03/F05/F06/F09; precedes F13; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F13-T1](2026-09-07-13-graph-monitoring-intervention.md#task-1-typed-lifecycle-and-control-receipts) | Typed lifecycle and control receipts | W4 after F12/F03/F07/F09/F06; F14 owned diffs later; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F13-T2](2026-09-07-13-graph-monitoring-intervention.md#task-2-safe-scheduler-boundaries-and-real-stopdrain) | Safe scheduler boundaries and real stop/drain | W4 after F12/F03/F07/F09/F06; F14 owned diffs later; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F13-T3](2026-09-07-13-graph-monitoring-intervention.md#task-3-node-retry-and-descendant-evidence-invalidation) | Node retry and descendant evidence invalidation | W4 after F12/F03/F07/F09/F06; F14 owned diffs later; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F13-T4](2026-09-07-13-graph-monitoring-intervention.md#task-4-node-focus-public-contract-inspection-and-controls) | Node focus, public contract inspection, and controls | W4 after F12/F03/F07/F09/F06; F14 owned diffs later; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F13-T5](2026-09-07-13-graph-monitoring-intervention.md#task-5-persistence-noninteractive-parity-and-control-acceptance) | Persistence, noninteractive parity, and control acceptance | W4 after F12/F03/F07/F09/F06; F14 owned diffs later; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F14-T1](2026-09-07-14-graph-advanced-controls.md#task-1-immutable-history-source-identity-and-retention) | Immutable history, source identity, and retention | W5 after F12/F13/F04/F05/F06/F09; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F14-T2](2026-09-07-14-graph-advanced-controls.md#task-2-read-only-diff-and-explain) | Read-only diff and explain | W5 after F12/F13/F04/F05/F06/F09; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F14-T3](2026-09-07-14-graph-advanced-controls.md#task-3-zero-worker-dry-run-preflight) | Zero-worker dry-run preflight | W5 after F12/F13/F04/F05/F06/F09; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F14-T4](2026-09-07-14-graph-advanced-controls.md#task-4-fork-and-rewind-through-shared-task-services) | Fork and rewind through shared task services | W5 after F12/F13/F04/F05/F06/F09; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F14-T5](2026-09-07-14-graph-advanced-controls.md#task-5-current-source-verify-only-execution) | Current-source verify-only execution | W5 after F12/F13/F04/F05/F06/F09; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F14-T6](2026-09-07-14-graph-advanced-controls.md#task-6-authorized-budget-inspection-and-adjustment) | Authorized budget inspection and adjustment | W5 after F12/F13/F04/F05/F06/F09; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |
| [F14-T7](2026-09-07-14-graph-advanced-controls.md#task-7-redacted-declarative-export-and-complete-command-regression) | Redacted declarative export and complete command regression | W5 after F12/F13/F04/F05/F06/F09; preceding task interfaces where consumed | not started | Required production adapters, additional scenarios, review and task commit outstanding |

Counts reconciled from source headings: **74 tasks / 75 requirements**. Verified: **0 tasks / 0 requirements**.

## Requirements

| Requirement | Required behavior | Implementing tasks / planned entry points | Status / evidence gap |
|---|---|---|---|
| F01-1 | Every policy Ask gets one typed challenge that explains action, target, reason, and stopping mode/policy. | Task 1: `f01_legal_scope`; Task 3: `f01_modal_traps_mode_key`; Task 5: `f01_noninteractive_fail_closed` | in progress; typed challenge, action/target/reason/policy display and contract tests f01_legal_scope, f01_modal_traps_mode_key, f01_noninteractive_fail_closed verified |
| F01-2 | The policy engine controls legal one-shot/session/project/denial choices; high-risk actions do not automatically gain persistent choices. | Task 1: `f01_legal_scope`; Task 2: `f01_stale_reply`; Task 4: `f01_grant_binding` | in progress; policy-owned scopes, risk exclusion, and contract tests f01_legal_scope, f01_stale_reply, f01_grant_binding verified |
| F01-3 | Up/Down and numbered options select, Enter explicitly confirms, and Esc denies/cancels. | Task 3: `f01_modal_traps_mode_key`; Task 5: `f01_noninteractive_fail_closed` | in progress; navigation/numeric selection, explicit Enter, Esc denial and contract tests f01_modal_traps_mode_key, f01_noninteractive_fail_closed verified |
| F01-4 | The modal traps Shift+Tab, ordinary composer keys, paste, and voice insertions without losing the preexisting draft. | Task 3: `f01_modal_traps_mode_key`; Task 5: `f01_noninteractive_fail_closed` | in progress; Shift+Tab/composer/paste/voice key trapping, draft preservation and contract tests f01_modal_traps_mode_key, f01_noninteractive_fail_closed verified |
| F01-5 | All hosts fail closed on stale replies, cancellation, transport loss, invalid scope, or storage failure; persistent grants remain narrowly scoped. | Task 2: `f01_stale_reply`; Task 4: `f01_grant_binding`; Task 5: `f01_noninteractive_fail_closed` | in progress; fail-closed on stale replies, cancellation, transport loss, storage failure and contract tests f01_stale_reply, f01_grant_binding, f01_noninteractive_fail_closed verified |
| F02-1 | Investigate before asking; require a materiality rationale and repository evidence references. | Task 1: `f02_question_bounds`; Task 3: `f02_waiting_mode` | not started; integration and all required proof outstanding |
| F02-2 | Provide 2–4 concrete choices where possible, an optional recommendation, and an appropriate custom-answer route. | Task 1: `f02_question_bounds`; Task 4: `f02_enter_required` | not started; integration and all required proof outstanding |
| F02-3 | Expose ask_user_question and persist explicit structured answer state in Living Plan. | Task 2: `f02_recommendation_not_answer`; Task 3: `f02_waiting_mode`; Task 4: `f02_enter_required`; Task 5: `f02_answer_invalidates_approval` | not started; integration and all required proof outstanding |
| F02-4 | Only an authenticated user selection creates AnsweredByUser; recommendation is not consent. | Task 2: `f02_recommendation_not_answer`; Task 3: `f02_waiting_mode`; Task 4: `f02_enter_required`; Task 5: `f02_answer_invalidates_approval` | not started; integration and all required proof outstanding |
| F02-5 | Preserve Open/AnsweredByUser/Deferred/Cancelled across resume and invalidate affected stale plan approvals. | Task 2: `f02_recommendation_not_answer`; Task 5: `f02_answer_invalidates_approval` | not started; integration and all required proof outstanding |
| F03-1 | Persist id/title/status/owner/dependencies/parent plan step/evidence references/timestamps separately from the Living Plan. | Task 1: `f03_public_status`; Task 3: `f03_no_publish_without_commit`; Task 5: `f03_resume_lineage` | in progress | Metadata/public vocabulary, durable persistence and live board projection tested; record-less recovery and cross-feature gates open |
| F03-2 | Support pending/in_progress/completed/blocked/failed/cancelled in the UI without destroying internal readiness semantics. | Task 1: `f03_public_status`; Task 5: `f03_resume_lineage` | in progress | Public status mapping to TUI states (Pending/Ready -> Queued, Running -> Active, Completed -> Done, Failed/Blocked -> Failed, Cancelled -> Skipped) verified in task board projection |
| F03-3 | Expose task_create/task_update/task_list/task_get and live board snapshots. | Task 4: `f03_dependency_gate`; Task 5: `f03_resume_lineage` | in progress | task_create/update/list/get exposed with coordinator transport; live board snapshots projected via task_board adapter and rendered in TUI |
| F03-4 | Only the owning worker or authorized controller can mutate; claims and handoffs are atomic and revision checked. | Task 2: `f03_claim_race`; Task 3: `f03_no_publish_without_commit`; Task 4: `f03_dependency_gate` | not started; integration and all required proof outstanding |
| F03-5 | Resume dependencies, ownership history, and evidence references safely; completion must not be inferred from plan acceptance. | Task 3: `f03_no_publish_without_commit`; Task 4: `f03_dependency_gate`; Task 5: `f03_resume_lineage` | in progress | Saved lineage, orphan reconciliation, recordful observer import, branch isolation and plan-acceptance-non-completion verified in live board projection; record-less recovery and full cross-feature proof outstanding |
| F04-1 | Checkpoint before substantial mutations and at verified milestones with task-owned effect provenance. | Task 1: `f04_checkpoint_required`; Task 2: `f04_restore_classification` | not started; integration and all required proof outstanding |
| F04-2 | Protect preexisting dirty content and later manual changes; overlapping edits must conflict rather than overwrite. | Task 1: `f04_checkpoint_required`; Task 2: `f04_restore_classification`; Task 3: `f04_stale_preview` | not started; integration and all required proof outstanding |
| F04-3 | Provide code-only, task/conversation-state-only, or combined restore selections. | Task 4: `f04_restore_selection`; Task 5: `f04_external_effect_not_undo` | not started; integration and all required proof outstanding |
| F04-4 | Separate reversible local effects from irreversible publish/deploy or other external effects. | Task 5: `f04_external_effect_not_undo` | not started; integration and all required proof outstanding |
| F04-5 | Make rewind previewable, revision checked, crash recoverable, and evidence invalidating without deleting history. | Task 3: `f04_stale_preview`; Task 4: `f04_restore_selection`; Task 5: `f04_external_effect_not_undo` | not started; integration and all required proof outstanding |
| F05-1 | Bind writable/protected scope, dependency policy, external action policy, and verification requirements to an accepted task. | Task 1: `f05_protected_wins`; Task 3: `f05_unknown_effects_blocked`; Task 5: `f05_requirement_not_skippable` | in progress; Task 1 contract schema/scope/digest and Task 3 effect boundary are verified. Process-capable contracted execution is explicitly unavailable rather than falsely treated as sandboxed; Task 5 remains pending. |
| F05-2 | Enforce scope on every mutation including batch, aliases, shell, MCP/native extensions, and children. | Task 2: `f05_modes_do_not_bypass_scope`; Task 3: `f05_unknown_effects_blocked` | verified | contract_gate, member-level batch enforcement, dispatch-time recheck, child worker context contract digest, effect_profile_allows, shell policy effect classification, process/network default-deny, and all 15 combined required fixture scenarios verified |
| F05-3 | Require an explicit contract revision for expansion, with approve/in-scope alternative/deny-with-instructions choices. | Task 4: `f05_expansion_requires_user` | not started; integration and all required proof outstanding |
| F05-4 | Always Approve never bypasses task ownership/contracts or hard filesystem/platform boundaries. | Task 1: `f05_protected_wins`; Task 2: `f05_modes_do_not_bypass_scope`; Task 3: `f05_unknown_effects_blocked`; Task 4: `f05_expansion_requires_user`; Task 5: `f05_requirement_not_skippable` | in progress; path scope, contract gate, member batches and child-worker contract inheritance are enforced under all modes; unsupported contracted process effects fail closed before spawn. Task 4 expansion and Task 5 audit remain pending. |
| F05-5 | Provide auditable, race-safe enforcement and a clearly stated limitation for effects that cannot be contained. | Task 2: `f05_modes_do_not_bypass_scope`; Task 3: `f05_unknown_effects_blocked`; Task 4: `f05_expansion_requires_user`; Task 5: `f05_requirement_not_skippable` | in progress; contract_gate, poisoned mutex recovery, execution_contract_unenforceable limitation, shell substitution detection verified; Task 4 expansion and Task 5 audit pending |
| F06-1 | Record command/tool provenance, execution outcome, relevant source fingerprint, useful runtime versions, and artifacts. | Task 1: `f06_source_must_match`; Task 2: `f06_no_skipped_success`; Task 5: `f06_report_gap` | not started; integration and all required proof outstanding |
| F06-2 | Mark evidence stale when relevant inputs change and reject prior-source success as current proof. | Task 1: `f06_source_must_match`; Task 3: `f06_required_dimensions`; Task 5: `f06_report_gap` | not started; integration and all required proof outstanding |
| F06-3 | Distinguish implemented/tested/built/installed/live-verified and show remaining gaps. | Task 3: `f06_required_dimensions`; Task 4: `f06_installed_hash_match`; Task 5: `f06_report_gap` | not started; integration and all required proof outstanding |
| F06-4 | Reserve verification/handoff resources and refuse proof-backed completion without required current evidence. | Task 3: `f06_required_dimensions`; Task 4: `f06_installed_hash_match` | not started; integration and all required proof outstanding |
| F06-5 | Use host-authenticated evidence with precise source coverage and no false success from skipped/unavailable checks. | Task 1: `f06_source_must_match`; Task 2: `f06_no_skipped_success`; Task 3: `f06_required_dimensions`; Task 5: `f06_report_gap` | not started; integration and all required proof outstanding |
| F07-1 | Display current activity, owned paths, dependencies/waits, elapsed time, tool/usage counts for every active worker. | Task 1: `f07_stale_control_rejected`; Task 5: `f07_panel_actions` | not started; integration and all required proof outstanding |
| F07-2 | Steer one active worker without restarting the task and show redirect versus queued-follow-up semantics. | Task 2: `f07_steering_delivery`; Task 5: `f07_panel_actions` | not started; integration and all required proof outstanding |
| F07-3 | Stop one worker or its task-owned process tree with real execution acknowledgment. | Task 3: `f07_stop_requires_exit`; Task 5: `f07_panel_actions` | not started; integration and all required proof outstanding |
| F07-4 | Inspect recent tool calls and owned diffs; retry safely without resetting unrelated work. | Task 1: `f07_stale_control_rejected`; Task 4: `f07_retry_lease`; Task 5: `f07_panel_actions` | not started; integration and all required proof outstanding |
| F07-5 | Enforce write ownership and explicit handoff through the runtime rather than prompts. | Task 1: `f07_stale_control_rejected`; Task 2: `f07_steering_delivery`; Task 3: `f07_stop_requires_exit`; Task 4: `f07_retry_lease` | not started; integration and all required proof outstanding |
| F08-1 | Show the final prepared request context and why each item is present, including token breakdown. | Task 1: `f08_total_includes_mandatory`; Task 4: `f08_overlay_next_request`; Task 5: `f08_visible_freshness` | not started; integration and all required proof outstanding |
| F08-2 | Support inspect/pin/exclude/refresh with explicit next-boundary application and bounded context. | Task 2: `f08_cannot_exclude_policy`; Task 4: `f08_overlay_next_request`; Task 5: `f08_visible_freshness` | not started; integration and all required proof outstanding |
| F08-3 | Distinguish user decision, repository fact, agent inference, and tool evidence provenance and freshness. | Task 1: `f08_total_includes_mandatory`; Task 3: `f08_inference_not_fact`; Task 5: `f08_visible_freshness` | not started; integration and all required proof outstanding |
| F08-4 | Allow explicit user correction/removal of memory without silent reinjection or inference promotion. | Task 3: `f08_inference_not_fact`; Task 5: `f08_visible_freshness` | not started; integration and all required proof outstanding |
| F08-5 | Mandatory system/permission/trust/safety context cannot be excluded. | Task 1: `f08_total_includes_mandatory`; Task 2: `f08_cannot_exclude_policy`; Task 4: `f08_overlay_next_request`; Task 5: `f08_visible_freshness` | not started; integration and all required proof outstanding |
| F09-1 | Share token/time/concurrency/retry/cost accounting across parent, descendants, reviews, verification, and provider retries. | Task 1: `f09_implementation_preserves_reserve`; Task 2: `f09_charge_once`; Task 5: `f09_unknown_cost` | not started; integration and all required proof outstanding |
| F09-2 | Show cost as unknown where pricing/usage is unavailable; do not reset budgets on recursion or retry. | Task 1: `f09_implementation_preserves_reserve`; Task 2: `f09_charge_once`; Task 5: `f09_unknown_cost` | not started; integration and all required proof outstanding |
| F09-3 | Detect repeated unchanged reads/commands, oscillating edits, and failed tests without a new experiment. | Task 3: `f09_repeated_work`; Task 5: `f09_unknown_cost` | not started; integration and all required proof outstanding |
| F09-4 | Offer Continue within remaining budget, return to Plan Mode with evidence, or stop preserving a checkpoint. | Task 4: `f09_continue_cannot_bypass_ceiling`; Task 5: `f09_unknown_cost` | not started; integration and all required proof outstanding |
| F09-5 | Reserve verification and handoff capacity and enforce limits under concurrency, crash/replay, and unknown usage. | Task 1: `f09_implementation_preserves_reserve`; Task 2: `f09_charge_once`; Task 4: `f09_continue_cannot_bypass_ceiling`; Task 5: `f09_unknown_cost` | not started; integration and all required proof outstanding |
| F10-1 | Support definitions, references, outline, diagnostics, rename preview, and call hierarchy where advertised. | Task 1: `f10_capability_fallback`; Task 3: `f10_utf16_boundaries`; Task 5: `f10_rename_preconditions` | not started; integration and all required proof outstanding |
| F10-2 | Keep ordinary file/text search available when semantic support is unavailable or incomplete. | Task 1: `f10_capability_fallback`; Task 4: `f10_server_launch_authority` | not started; integration and all required proof outstanding |
| F10-3 | Manage server startup, capabilities, cancellation, framing, resource bounds, and workspace trust safely. | Task 1: `f10_capability_fallback`; Task 2: `f10_length_counts_bytes`; Task 4: `f10_server_launch_authority` | not started; integration and all required proof outstanding |
| F10-4 | Bind locations/diagnostics/edits to document versions and source fingerprints, including Unicode/CRLF handling. | Task 3: `f10_utf16_boundaries`; Task 5: `f10_rename_preconditions` | not started; integration and all required proof outstanding |
| F10-5 | Rename changes require normal permissions/contracts/checkpointing and verification; LSP output is not proof of correctness. | Task 5: `f10_rename_preconditions` | not started; integration and all required proof outstanding |
| F11-1 | Launch Davinci in a controlled terminal, send keys and resize, and assert rendered state. | Task 1: `f11_backend_capabilities`; Task 2: `f11_resize_bounds`; Task 3: `f11_key_encoding`; Task 4: `f11_five_mode_cycle` | not started; integration and all required proof outstanding |
| F11-2 | Cover five-mode Shift+Tab cycling with an unfinished draft preserved and ordinary Tab behavior unchanged. | Task 3: `f11_key_encoding`; Task 4: `f11_five_mode_cycle` | not started; integration and all required proof outstanding |
| F11-3 | Record screen frames, event logs, process exits, and exact executable/source identity as evidence. | Task 2: `f11_resize_bounds`; Task 3: `f11_key_encoding`; Task 4: `f11_five_mode_cycle`; Task 6: `f11_evidence_environment` | not started; integration and all required proof outstanding |
| F11-4 | For web tasks, assert browser state and capture screenshots/DOM, console errors, failed requests, and useful traces. | Task 1: `f11_backend_capabilities`; Task 5: `f11_network_fixture_only`; Task 6: `f11_evidence_environment` | not started; integration and all required proof outstanding |
| F11-5 | Run reproducibly with isolated resources, no live services/downloads, bounded artifacts, and no false claim of physical-keyboard verification. | Task 1: `f11_backend_capabilities`; Task 2: `f11_resize_bounds`; Task 3: `f11_key_encoding`; Task 5: `f11_network_fixture_only`; Task 6: `f11_evidence_environment` | not started; integration and all required proof outstanding |
| F12-1 | Implement /graph save <name> and /graph run <name> with .davinci/graphs/<name>.yaml storage. | Task 1: `f12_safe_graph_name`; Task 4: `f12_overwrite_requires_explicit`; Task 5: `f12_reserved_word_escape`; Task 6: `f12_replay_requires_all_bindings` | not started; integration and all required proof outstanding |
| F12-2 | Save topology, roles, artifact contracts, budgets, verification policy, and safe parameters only. | Task 2: `f12_role_artifact_coherence`; Task 3: `f12_frozen_definition`; Task 4: `f12_overwrite_requires_explicit`; Task 6: `f12_replay_requires_all_bindings` | not started; integration and all required proof outstanding |
| F12-3 | Reject unsafe/malformed definitions and preserve DAG, one-writer, mode-specific verification/security/review gates. | Task 1: `f12_safe_graph_name`; Task 2: `f12_role_artifact_coherence`; Task 3: `f12_frozen_definition`; Task 6: `f12_replay_requires_all_bindings` | not started; integration and all required proof outstanding |
| F12-4 | Execute the saved definition through real native bindings without silently reclassifying/replacing it. | Task 3: `f12_frozen_definition`; Task 6: `f12_replay_requires_all_bindings` | not started; integration and all required proof outstanding |
| F12-5 | Do not save hidden reasoning, transcripts, ad-hoc prompts, credentials, or dynamic workflow scripts. | Task 1: `f12_safe_graph_name`; Task 2: `f12_role_artifact_coherence`; Task 4: `f12_overwrite_requires_explicit`; Task 6: `f12_replay_requires_all_bindings` | not started; integration and all required proof outstanding |
| F12-6 | Preserve bare /graph and legacy goal/flag behavior with an explicit escape for reserved subcommand words. | Task 5: `f12_reserved_word_escape`; Task 6: `f12_replay_requires_all_bindings` | not started; integration and all required proof outstanding |
| F13-1 | Show phases, worker states, time/usage, outputs, dependencies, and blocked reasons in a dedicated graph view. | Task 1: `f13_pause_ack_boundary`; Task 4: `f13_graph_keys`; Task 5: `f13_restart_never_resurrects_worker` | not started; integration and all required proof outstanding |
| F13-2 | Pause/resume at safe boundaries and never start new nodes after an acknowledged pause. | Task 1: `f13_pause_ack_boundary`; Task 2: `f13_no_dispatch_when_paused`; Task 4: `f13_graph_keys`; Task 5: `f13_restart_never_resurrects_worker` | not started; integration and all required proof outstanding |
| F13-3 | Stop/retry a selected node without restarting compatible unrelated successes. | Task 2: `f13_no_dispatch_when_paused`; Task 3: `f13_invalidate_descendants`; Task 4: `f13_graph_keys`; Task 5: `f13_restart_never_resurrects_worker` | not started; integration and all required proof outstanding |
| F13-4 | Inspect public prompt contracts, tool activity, artifacts, verification, and owned diffs. | Task 4: `f13_graph_keys`; Task 5: `f13_restart_never_resurrects_worker` | not started; integration and all required proof outstanding |
| F13-5 | Preserve deterministic ownership, one writer, bounded fan-out, current evidence, and durable lifecycle controls. | Task 1: `f13_pause_ack_boundary`; Task 2: `f13_no_dispatch_when_paused`; Task 3: `f13_invalidate_descendants`; Task 5: `f13_restart_never_resurrects_worker` | not started; integration and all required proof outstanding |
| F14-1 | Diff original/prior/current validated graph/state and separately present owned code changes. | Task 2: `f14_read_operations_do_not_dispatch`; Task 7: `f14_export_allowlist` | not started; integration and all required proof outstanding |
| F14-2 | Fork from a selected node with a different bounded strategy while preserving compatible earlier evidence. | Task 1: `f14_replay_content_not_status`; Task 4: `f14_branch_keeps_spend` | not started; integration and all required proof outstanding |
| F14-3 | Rewind to the task-owned checkpoint before a writer without overwriting dirty/manual edits. | Task 1: `f14_replay_content_not_status`; Task 4: `f14_branch_keeps_spend` | not started; integration and all required proof outstanding |
| F14-4 | Explain node purpose, dependencies, permissions, artifacts, and exit criteria deterministically. | Task 2: `f14_read_operations_do_not_dispatch`; Task 7: `f14_export_allowlist` | not started; integration and all required proof outstanding |
| F14-5 | Dry-run validates topology/budgets/scopes/roles/permissions without executing workers. | Task 3: `f14_preflight_zero_effects`; Task 7: `f14_export_allowlist` | not started; integration and all required proof outstanding |
| F14-6 | Verify-only runs current-source verification/review without implementation workers or simulated proof. | Task 5: `f14_verify_never_runs_writer`; Task 7: `f14_export_allowlist` | not started; integration and all required proof outstanding |
| F14-7 | Inspect/adjust only authorized root resource ceilings without resetting cumulative usage. | Task 6: `f14_budget_floor`; Task 7: `f14_export_allowlist` | not started; integration and all required proof outstanding |
| F14-8 | Export the validated declarative definition for review/reuse, excluding private transient content. | Task 7: `f14_export_allowlist` | not started; integration and all required proof outstanding |
| F14-9 | All operations preserve immutable history, revision checks, exact replay identity, and mode-specific native safety gates. | Task 1: `f14_replay_content_not_status`; Task 2: `f14_read_operations_do_not_dispatch`; Task 3: `f14_preflight_zero_effects`; Task 4: `f14_branch_keeps_spend`; Task 5: `f14_verify_never_runs_writer`; Task 6: `f14_budget_floor`; Task 7: `f14_export_allowlist` | not started; integration and all required proof outstanding |

## Final verification and delivery gates

- [ ] `rtk cargo fmt --check`: passed on partial checkpoints; must rerun on final integrated source.
- [ ] `rtk cargo test -p davinci-agent --offline`: not run on expansion implementation.
- [ ] `rtk cargo test -p davinci-coding-agent --offline`: not run on expansion implementation.
- [ ] `rtk cargo test -p davinci-tui --offline`: not run on expansion implementation.
- [ ] `rtk cargo test --workspace --offline`: not run on expansion implementation.
- [ ] `rtk cargo clippy --workspace --all-targets --offline -- -D warnings`: not run on expansion implementation.
- [ ] `rtk cargo build -p davinci-coding-agent --offline`: not run on expansion implementation.
- [ ] `rtk cargo build -p davinci-coding-agent --release --offline`: not run on expansion implementation.
- [ ] Combined native/legacy/print/JSON/RPC behavior, old session migration, authenticated decisions, scope/batch/aliases, cancellation/crash recovery, current-source evidence and graph safety: unverified.
- [ ] Dependency/MSRV/license/advisory admission and real optional backend availability: not established. No downloads authorized implicitly by tests.
- [ ] Windows and Unix PTY/owned process-tree acceptance, browser fixture acceptance and physical-keyboard manual checks: unverified; no simulation substitutes.
- [ ] Resolve normal installed command, back up binary, install offline release without stopping user sessions, hash-match build and installed binary, smoke-test resolved installed path: not performed.

## Current handoff

Continue F03 Tasks 1-3 through the authenticated task command coordinator and host session binding. The registry now has an optional authoritative journal and exact revision/result/cascade replay; existing product startup still constructs an ephemeral registry. Add canonical operation-id replay/response semantics and authenticated CAS before activating the host adapter. Session switches must refresh both Agent.runtime and ToolContext.runtime, select the intended lineage and rebind persistence rather than overlaying unrelated task maps. Graph children currently use no-session startup with a fresh runtime identity; parent-coordinator transport and ownership identity must be wired, not inferred from model JSON. Lineage/plan resolution, versioned durable observations, resume fencing and the remaining recovery/platform gates stay incomplete. No external blocker currently prevents this work.

## Partial metadata checkpoint evidence

- Intended RED: missing metadata/status API compile errors; then oversized real-registry create assertion failed (2 passed/1 failed); then missing public_status tool assertion failed (5 passed/1 failed); then old tool title precheck assertion failed (0 passed/1 failed). These failures are feature assertions, not cache/toolchain failures.
- GREEN: `rtk cargo test -p davinci-agent --offline --locked runtime:: -- --nocapture`: exit 0, 99 passed / 275 filtered. Nine new F03 tests plus existing runtime tests.
- `rtk cargo fmt --check`: exit 0 after review fixes.
- `rtk cargo check -p davinci-coding-agent --offline --locked`: exit 0, 169 crates checked before the final title adapter change. No app release or live acceptance is implied.
- Independent review found public-filter/schema mismatch and replay bounds bypass. Fixed with two tests observed red (7 passed/2 failed), then green. Added an explicit public_status filter while retaining internal status semantics; prevalidated the complete replay input before projection changes. Final independent review: no remaining findings in the three-file partial checkpoint; scoped diff check passed. F03 Task 1 remains in progress. No task or requirement marked verified.
- Source identity for the metadata checkpoint runtime test (SHA-256 of exact file bytes):
  - `crates/davinci-agent/src/runtime/tasks.rs`: `d388011f4c8994e1ffadd7b5afa485dbab9e3c54fb319e89973f8e636eb785d1`
  - `crates/davinci-agent/src/runtime/ids.rs`: `4e61eca90a625f5c2b35f66ffdaa0c374a96ae55167df117e8109d0a5847020b`
  - `crates/davinci-agent/src/runtime/tools_task.rs`: `43b56a1ef515ddc10b29c7d0e5e845c6c58c1f608c01f0c0f72b2e6abb4cf436`

- `rtk cargo clippy -p davinci-agent --all-targets --offline --locked -- -D warnings`: exit 0 after review fixes.
- `rtk git diff --check` for all three changed source files: exit 0.
- Plans/specifications/tracker checkpoint: `fee0a37`; no push or installation.

## Partial revision checkpoint evidence

- Metadata implementation checkpoint: `3d5a6f34488368807d25bf9d4365167cfada63b5`.
- RED: `rtk cargo test -p davinci-agent --offline --locked f03_ -- --nocapture` failed on the missing `with_clock` API.
- GREEN: the same F03 selector passed 11 tests / 365 filtered; supplemental cascade-overflow, callback-lock and intervening-cancellation cases then passed in `rtk cargo test -p davinci-agent --offline --locked runtime:: -- --nocapture`: exit 0, 104 passed / 275 filtered.
- `rtk cargo fmt --check` and `rtk cargo clippy -p davinci-agent --all-targets --offline --locked -- -D warnings`: passed.
- `rtk cargo check -p davinci-coding-agent --offline --locked`: exit 0, four crates checked after this source change.
- Independent reviewer checked the scoped diff and callers against `3d5a6f3`, ran scoped diff check, and found no issues. Ready for this bounded checkpoint only.
- Revision/timestamp changes are atomic in memory, including dependent cascades. Completion revalidates the pre-hook task revision. This is not durable commit proof, owner authorization or evidence-backed task completion.
- Replay still uses the legacy observer log; restoring exact revisions and separating proposed completion from committed observations are required F03 T1/T3 integration work.
- Exact tested `crates/davinci-agent/src/runtime/tasks.rs` SHA-256: `b20664c84f2b60deb60b1e98b34a272d0518d4bc50ebea5eedc5cf16cdcda1c4`. Other source files unchanged from metadata checkpoint.
- Next implementation boundary: one authenticated task command coordinator over the existing registry, with durable append/sync before projection/observations. Replace team check-then-claim and multi-step tool updates through it; select restored lineage explicitly. Existing callback bus must remain outside registry/storage locks.

## Partial completion-event checkpoint evidence

- Base revision: `616719c` (atomic task revision/cascade checkpoint).
- `TaskCompletionRequested` is the vetoable proposal and carries the expected revision. Successful `TaskCompleted` is observe-only and emitted after projection publication. Legacy `taskCompleted` shell hooks retain the old `event.kind` and `success` fields, with additive proposal phase/revision fields at that adapter only.
- RED: rejected-completion and published-result/cascade tests each selected one test and failed their intended assertions before the change. A shell stdin fixture then failed because the proposal changed the legacy hook event name.
- GREEN: `rtk cargo test -p davinci-agent --offline --locked runtime:: -- --nocapture`: 107 passed / 275 filtered.
- `rtk cargo test -p davinci-coding-agent --offline --locked f03_ -- --nocapture`: four passed / 1392 filtered across lib/bin targets before the two supplemental legacy-payload tests.
- `rtk cargo test -p davinci-coding-agent --offline --locked runtime_host::tests:: -- --nocapture`: 20 passed / 1376 filtered across lib/bin targets, including actual temporary observer-log write/read/replay of rejected completion.
- Final hook suite: `rtk cargo test -p davinci-coding-agent --offline --locked hooks::tests:: -- --nocapture`: 48 passed / 1352 filtered across lib/bin targets. Includes a real fixture-controlled shell hook denying completion through HooksRuntimeSubscriber based on the legacy kind/success payload.
- `rtk cargo fmt --check`, scoped `rtk git diff --check`, and `rtk cargo clippy -p davinci-agent --all-targets --offline --locked -- -D warnings`: passed.
- Independent review identified the external hook payload regression; it was fixed and retested. Final review found no remaining issues for this bounded checkpoint. Reviewer independently passed the three native F03 adapter tests in lib/bin targets and all 17 agent F03 tests.
- This does not make the secondary observer log authoritative or durable. Journal append/sync, writer lease, exact replay, authenticated CAS and evidence-backed completion remain open. Verified counts remain 0/74 tasks and 0/75 requirements; no executable installed or pushed.
- Exact source byte SHA-256 values for this checkpoint:
  - `crates/davinci-agent/src/runtime/tasks.rs`: `48958bd884a8371d8979e805ffa8b80f9aad1c771419ce2a848c8896e6373410`
  - `crates/davinci-agent/src/runtime/events.rs`: `0669bb731aaddda3dab5ed0465244ab783d9e27d39ac6c199c83dcbaf581d08d`
  - `crates/davinci-agent/src/runtime/bus.rs`: `f172f96303521192bb1dda9b8a32df5f881d88305a06754a7b2e4fa5ac1d7b3c`
  - `crates/davinci-coding-agent/src/hooks.rs`: `d413dbc7f561c7203774e10d2dc88ee7168aa04a938c796be94e91af5a340f4b`
  - `crates/davinci-coding-agent/src/runtime_host.rs`: `879e32a6589becd8dbb37e95c7ac5ed7a2a1842250334069a863d06a21bf72e7`

## Partial authoritative-journal checkpoint evidence

- Base revision: `1a7524f`. Added `runtime/task_store.rs`; changed `runtime/tasks.rs` and module registration only.
- `TaskRegistry::open_durable(path, run_id)` opens a single OS-held writer lease and replays a bounded checksummed schema-v1 transaction journal. All five existing registry mutation paths persist candidate deltas and sync the file before publishing. Completion hooks remain outside the write lane; observer failures do not undo commits. Legacy observer replay cannot overwrite a journal-backed registry.
- Bounds: 1 MiB per encoded frame and 64 MiB per journal. Encoding itself is bounded. Capacity errors reject the candidate without publishing; retention/compaction and host-facing recovery UX are not implemented.
- RED: the durable registry test failed on the missing `open_durable` API. The first recovery suite then selected 23 F03 tests: 22 passed and the torn-tail case failed. Diagnostic reproduction located Windows error 5 specifically at `set_len` on an append-only handle. Fixed by opening with write access and serializing explicit end seeks under the existing exclusive lease/mutex.
- Additional RED: unknown-schema complete JSON without a final newline was incorrectly quarantined; its regression selected one test and failed. Complete JSON tails now receive schema/lineage/sequence/checksum validation before quarantine.
- Final GREEN: `rtk cargo test -p davinci-agent --offline --locked runtime:: -- --nocapture`: 118 passed / 275 filtered. Includes actual temporary journal writes/reopens, result/assignment/cancellation/failure and transitive-block replay, second writer denial/release, exact torn-byte quarantine, corrupt middle/schema/run/duplicate rejection, bounded encoding, observer panic and injected before-write/partial-write/after-sync failures. The injected disk-full sink proves no registry publication for any mutation path; it is not a real full-disk experiment.
- `rtk cargo fmt --check`, `rtk cargo clippy -p davinci-agent --all-targets --offline --locked -- -D warnings`, and scoped diff checks passed. Final `rtk cargo check -p davinci-coding-agent --offline --locked` passed (four crates checked).
- Independent reviewer read the new untracked journal directly, checked the registry boundary, and passed focused journal/no-publication/durable tests plus 27 F03 tests. No findings in that bounded snapshot. Supplemental unknown-schema-tail review also found no issues and independently passed its regression (one selected test).
- Explicit gaps: product/session adapter remains ephemeral; operation IDs are generated per journal transaction, not yet authenticated caller idempotency keys; no ownership CAS, old-session migration, child command transport, running-owner fencing, fresh-process crash acceptance, Unix execution or physical power-loss durability claim. Observer notifications are secondary and are not yet a versioned durable commit subscription. No task/requirement is marked verified.
- Source SHA-256:
  - `crates/davinci-agent/src/runtime/tasks.rs`: `2f8e50609351319583cf0ead856cd47c34111d20e2530c43bbf9e9a929cf19ca`
  - `crates/davinci-agent/src/runtime/task_store.rs`: `b1c0ea791cced93a9a829a1e66f1dcdf612fd5468efd2c480beb969f798e89e4`
  - `crates/davinci-agent/src/runtime/mod.rs`: `2c9ea3da8bb8769eb418455b79066d694d1ddfc9562209df6a4163ec71e83f1b`

## Partial atomic-claim checkpoint evidence

- Base revision: `a5dda5f`. Added crate-private registry claim transaction and routed `TeamManager::claim_task` through it. Run identity comes from the host runtime; teammate membership is checked by the team. Under one write lock the registry checks run/revision/unowned/Ready state, increments owner generation and revision, persists the candidate, then publishes. Observers run after unlocking.
- RED: `f03_claim_race` selected the new production contract and failed to compile because `claim_task` did not exist. GREEN: two real threads sharing the same revision produce exactly one winner; a separate TeamManager barrier test verifies its production adapter. Pending, terminal, foreign-run and stale-revision rejections preserve the record. Both counter overflows preserve the record. The injected disk-full test now covers claim publication and observations too.
- Final runtime suite: `rtk cargo test -p davinci-agent --offline --locked runtime:: -- --nocapture` passed 122 tests / 275 filtered. Agent all-target Clippy passed with warnings denied. Product `cargo check` passed; formatting was applied and checked. No workspace/release/platform acceptance claimed.
- Independent reviewer found no actionable findings for the claim transaction and TeamManager adapter; independently passed claim, team-race and failed-persistence selectors. The final counter-overflow test was added after that review and passed locally.
- F03-T2 remains in progress. Legacy `assign_task` and completion paths still need owner authorization; model tool inputs need whole-command validation, runtime-bound identity, revision checks and durable canonical operation replay. Explicit fenced handoff depends on F07 process control. This checkpoint does not close F03-4 or any full task; verified totals remain 0/74 tasks and 0/75 requirements.
- Next: integrate authenticated command handling into `tools_task.rs` and TeamManager completion, then persist canonical operation IDs/results and activate the journal through correct session/runtime rebinding. Do not activate durable session replay using the current overlay/rebinding behavior.

## Partial owner-authorized update checkpoint evidence

- Base revision: `7d1f2e7`. Changed registry, TeamManager and task tool adapters. Crate-private `TaskOwner` carries host run/agent identity and expected revision/generation. Guarded completion/failure/cancellation reuse the existing transactional implementations. Completion validates before the decision hook and again before publication. TeamManager now rejects another teammate's completion without a lead mailbox notice.
- `task_update` derives identity from RuntimeHandle, requires unsigned `expected_revision`, rejects unknown/malformed fields, and routes a self-claim or owned terminal update through guarded methods. Combined assignment/status requests are rejected before mutation. Results over 65,536 UTF-8 bytes are rejected without slicing or truncation. Create/update/list responses expose revision and owner_generation. The lifecycle fixture now claims before completing.
- RED: the teammate ownership regression failed its intended assertion (one selected test); forged-owner and assignment-before-invalid-status regressions both failed (two selected tests). GREEN: `rtk cargo test -p davinci-agent --offline --locked runtime:: -- --nocapture` passed 128 tests / 275 filtered. Includes wrong run/agent/revision/generation, ownership change inside completion hook, malformed/unknown fields, stale revision and UTF-8 boundaries, and legitimate claim/complete/dependency lifecycle.
- Agent all-target Clippy with warnings denied and product `cargo check --offline --locked` passed. Independent reviewer found no actionable findings and passed two owned-status, three tool-validation and one teammate-ownership tests.
- Remaining: canonical operation-id replay/response persistence, scoped controller capability, create-time assignment policy, legacy host mutator migration, actual worker identity/session transport and F07-fenced handoff. TaskOwner is a host adapter contract, not an authenticated cross-process transport. Current tool response still reads the latest projection; exact original operation responses belong to the upcoming idempotency transaction. No full task or requirement is marked verified; all final integration/platform/install gates remain open.
- Next concrete work: extend the authoritative journal command envelope with canonical authenticated operation IDs and exact response replay, then route creation and remaining host/worker paths through that coordinator before session activation.

## Partial durable claim-replay checkpoint evidence

- Base revision: `f8c6fe3`. Added canonical ClaimRequest (task/run/host actor/expected revision), a bounded 4,096-entry receipt projection, and original TaskRecord responses. Receipt lookup and conflict detection run under the task write lane before revision/ownership mutation. Retries do not republish observations or change revisions. Conflicting payload/actor reuse fails closed.
- The authoritative journal writes the claim receipt and task change in the same checksummed schema-v2 frame. Schema-v1 frames remain readable with their original checksum serialization. Replay restores receipts; failed writes cannot populate the in-memory success cache. Existing frame/journal byte limits remain enforced. At receipt capacity, new idempotent claims reject while existing receipts remain replayable; compaction remains future work.
- The actual task_update adapter accepts optional operation_id for claims and returns the stored original response even after later task changes. Status operation IDs explicitly reject until status replay exists; this is temporary incomplete functionality, not completion of the command coordinator. Identity still comes from RuntimeHandle, not JSON actor fields.
- RED: durable claim replay test failed on missing claim_operation API. Final GREEN: `rtk cargo test -p davinci-agent --offline --locked runtime:: -- --nocapture` passed 134 tests / 275 filtered. Includes real temporary journal reopen, concurrent duplicate requests, changed actor/revision conflicts, exact tool response replay, refused storage without success caching, receipt capacity, and injected after-sync-before-publication recovery. The latter is a fixture, not fresh-process/power-loss acceptance.
- Agent all-target Clippy and product check passed; independent review found no actionable findings and passed focused claim, journal receipt, tool replay and malformed-input selectors. Supplemental capacity test passed locally after review. Formatting and scoped diff checks passed before commit.
- Remaining: generalized canonical command receipts for creation and terminal updates, mandatory operation-ID migration, controller capability, ownership/creation policy, legacy host mutator migration, session activation, worker transport and F07-fenced handoff. All full-task/requirement verified counts remain zero; final platform/build/install gates remain open.
- Next: generalize the claim receipt into the command receipt envelope, preserving v1/v2 replay, and carry it through the existing guarded status transaction without holding locks across hooks. Duplicate completion requests must not rerun completion hooks after a committed result.

## Partial durable status-replay checkpoint evidence

- Base revision: `491c1be`. Generalized internal request/receipt types and the shared operation-ID namespace to completion, failure and cancellation. Status requests bind run/actor/task/revision/generation/state/result; schema-v3 frames store the original response plus all dependency changes in one commit. Schema-v1/v2 replay and checksum serialization remain compatible; the historical `claim` wire field name is intentionally retained.
- Completion checks committed replay before decision hooks and again in the final write lane. Bounded in-flight operation reservations prevent simultaneous/reentrant duplicate hooks without retaining locks across callbacks. An unsettled duplicate receives OperationInProgress; after commit it receives the exact original response. Denial releases the reservation so retry can be evaluated again. Failed/cancelled operations use the same receipt projection and persistence boundary.
- Actual task_update now supports optional operation_id for all terminal statuses as well as claims. Exact original content/details are returned on replay. Cancellation rejects supplied result text; operation-ID migration is still optional and task_create is not yet replayable.
- RED: completion replay failed to compile on the missing complete_operation API. During implementation a mechanical type rename also touched the existing TaskCompletionRequested identifier; compilation identified it and the protected event spelling was restored. Final GREEN: `rtk cargo test -p davinci-agent --offline --locked runtime:: -- --nocapture` passed 139 tests / 275 filtered. Tests cover all three durable status/dependency reopens, original response replay, changed actor/result and cross-command conflicts, denied-hook reservation release, reentrant duplicate rejection, a real two-thread duplicate with exactly one hook, and tool status response replay.
- Formatting, agent all-target Clippy with warnings denied and product cargo check passed. This is scoped runtime/compile evidence; full workspace/native/print/RPC/session/process-crash/Unix/physical-interaction and installation gates remain open.
- Next: implement canonical task_create receipts (including generated ID/timestamp response), enforce creation/assignment lineage policy and mandatory command operation IDs, then route remaining controller/worker/session adapters through the coordinator. No full task or requirement is marked verified; the fourteen-plan objective remains active.

- Independent review completed with no remaining actionable findings. A proposed receipt-capacity race was withdrawn after confirming the second replay/capacity check holds the task write lane through persistence and insertion. Added a two-thread capacity regression: a paused completion loses the last receipt slot to a failure command, rejects before mutation, retains revision/state, and releases its reservation (1 passed / 414 filtered). This uses a seeded in-memory receipt projection; it is not journal-reopen or process-crash evidence.
- Final checkpoint rerun after the capacity regression: runtime selector 140 passed / 275 filtered; agent all-target Clippy with warnings denied and scoped diff check passed.

## Partial durable creation-replay checkpoint evidence

- Base revision: 2182fe7. Added a typed creation request containing only caller-supplied fields. The existing coordinator now stores the generated TaskRecord and canonical request together in schema-v4 receipts, sharing the bounded operation-ID namespace with claims and statuses. Old schema-v1/v2/v3 wire serialization remains compatible. Original ID, timestamps, revision and response survive retry/reopen even after later task updates.
- Actual task_create now supports optional operation_id and returns the original committed response directly, avoiding a latest-projection race. Unknown/malformed fields reject before mutation; caller identity comes from RuntimeHandle. Assignment is restricted to self and starts owner_generation at one. Creation rejects duplicate and foreign-run dependencies; legacy trusted create_task remains available for host migration.
- RED: f03_tool_creation_replays_original_response selected one test and failed because duplicate calls returned different generated task IDs. GREEN: runtime selector passed 147 tests / 275 filtered. Includes two-thread duplicate creation with one TaskCreated observation, real temporary journal reopen after later updates, cross-command/actor/payload conflict, malformed input, lineage/assignment rejection, and refused persistence without success caching. Injected after-sync-before-publication creation replay passed; it is not a real process-crash or power-loss test.
- Agent all-target Clippy with warnings denied, product cargo check (4 crates), and scoped diff check passed. Independent review is pending. Full workspace, native/print/JSON/RPC/session/worker integration, platform interaction, final builds and executable delivery remain open.
- Next: require command operation IDs in model tool adapters and migrate their fixtures/callers, then integrate the journal with the authorized session/worker coordinator. Controller capabilities and quiescent handoff remain owned follow-on work. No full task or requirement is marked verified.
- Review found a schema-v4 replay semantic-validation gap; a recomputed-checksum regression failed as intended. Live creation and replay now share dependency uniqueness/lineage/cycle validation and initial-state derivation against the pre-create projection. Regression passed, runtime rerun passed 148 / 275 filtered, and independent re-review found no remaining findings. Final formatting check passed. This closes the bounded creation checkpoint only.

## Partial F01 decision-subscriber enforcement checkpoint

- Base revision: 5d9a304. The active turn permission gate now propagates a runtime PermissionRequested rejection, emits PermissionDenied, and returns through the normal blocked-tool result path. It does not invoke the user callback or add a session rule after that rejection. This implements one explicit F01-T2 integration requirement, not the full challenge system.
- RED: offline actual-agent-loop write fixture failed because the callback ran despite subscriber refusal. GREEN: the focused regression passed; turn::tests passed 4 / 421 filtered. The regression verifies no callback, no file write, no session grant, one denial observation, and an error tool result. No live provider/network was used.
- Independent security/correctness review found no findings and reran the focused regression. Agent all-target Clippy passed on the combined creation/turn snapshot. Required broader challenge, policy, modal, RPC/legacy/print, cancellation and physical interaction gates remain open. F01-T2 is in progress and no requirement is marked verified.

## F03 model-tool operation-ID enforcement checkpoint

- Base revision: 1447c73. task_create and task_update now require operation_id in both advertised JSON schema and runtime validation. Missing IDs reject before mutation. Existing successful and malformed/ownership fixtures supply valid IDs so earlier assertions still reach their intended checks.
- RED: f03_task_commands_require_operation_ids failed because creation without an ID succeeded. GREEN: task-tool module 12 passed / 413 filtered; runtime module 149 passed / 276 filtered. Agent all-target Clippy passed. Independent review found no findings and reran all 12 task-tool tests.
- Creation/claim/status tool calls now use the bounded shared receipt path; trusted legacy host registry calls and TeamManager migration still need the authorized coordinator/session/worker integration. No entire F03 task or requirement is verified. Next executable work includes bounded task_get/list read adapters, journal activation and F01 policy-owned challenges.

## Partial F03 bounded read-adapter checkpoint

- Base revision: 2883ba2. Registered task_get through the existing runtime exports, tool dispatch, schemas and capability classification. RuntimeHandle supplies the run; foreign-run and missing tasks return the same error. Read output bounds result text to 4,096 UTF-8 bytes, description to 16 KiB, evidence references to 128 and blocked reasons to 16 bounded entries. Truncation flags expose omitted content without changing the stored record. Plan resolution and evidence validity remain follow-on integration work.
- task_list defaults to 50 records, accepts limits 1-100 and unsigned offsets, validates filters and rejects unknown fields. Task-ID ordering is deterministic for unchanged state; schemas explicitly document that concurrent changes can shift offsets. Responses expose page count, total and next_offset. Output is bounded, but the underlying legacy registry snapshot still clones matching records before pagination.
- RED: pagination failed its expected count assertion; task_get failed on its missing adapter symbol. GREEN: task-tool tests passed 14; latest runtime selector passed 151 / 277 filtered. Tests cover foreign-run exclusion, unchanged stored UTF-8 results, malformed filters, disjoint pages and maximum offset. Dispatch/capability gate fixture passed, as did read-only policy validation.
- Independent review found the optional task reads were still assigned to the serial extension lane. A preparation regression failed, then passed after recognizing these optional built-in implementations at lane selection. They were not added to BUILTIN_TOOLS because that list also enables default tools; the experimental dispatch/advertisement gate remains. Re-review found no remaining findings and reran lane, gating and permission tests.
- Agent all-target Clippy with warnings denied and product cargo check (4 crates) passed. This is a partial F03-T4 checkpoint; no full task or requirement is marked verified. Journal host/session activation, authenticated worker transport, F02/F06 integration, live board, all full workspace/platform/interaction/release/install gates remain open. Next: establish the session-owned journal/coordinator lifecycle and migrate its actual host adapters, with resume lineage and writer fencing tested before activation.

## F03 fresh-process journal validation and host activation audit

- Base revision: 737fc64. Added an offline exact-test subprocess fixture for the existing journal implementation. A writer process syncs a schema-v4 creation receipt through the after-sync fault boundary, signals readiness by renaming a completed fixture file, and parks while holding the OS writer lease. A separate contender process verifies writer exclusion. The parent kills and waits for that exact writer; another fresh process restores the exact original TaskRecord/receipt in the supplied lineage and verifies replay does not append. Child guards kill/wait on assertion failures; readiness and completion polls are bounded.
- This is a verification extension of existing behavior, so the new process test passed immediately; no invented RED or production-behavior change is claimed. On Windows, the focused process test passed, then all 14 task_store tests passed / 415 filtered. Agent all-target Clippy passed before the readiness-file atomic-rename refinement. Independent review found no findings and reran the process test. This proves process-kill/lease/reopen behavior at the injected after-sync boundary, not power-loss durability, Unix execution or product session activation.
- Current-state host audit confirms startup main.rs constructs a new run and WorkflowExecutor clone before binding the session, then ignores runtime-log read/replay errors. Agent::load_from_session mutates only the agent runtime's session_id and overlays observer state; tool_context.runtime and the session log subscriber can remain stale. RuntimeBus subscriptions are append-only, so simply adding another log subscriber would mix session logs. Native /new and extension newSession directly assign agent.session. RPC and SDK session loaders must also be included in the migration.
- Next integration sequence: prepare one complete session runtime through a host-owned fallible loader before mutating the active session; select persisted task lineage separately from arbitrary --session-id text; acquire the durable registry lease; attach observers to that registry; replay only non-task secondary projections; construct WorkflowExecutor after the final binding; then atomically install agent/tool-context runtimes and session messages. Use a fresh bus or explicit observer replacement. Any failure must preserve the previous active session/runtime. Reuse an already bound same-session registry when navigating within its session, rather than attempting a second writer lease. Do not activate durable tasks before legacy observer migration, resume ownership fencing and all direct session-assignment callers are handled.
- Actual integration callers from this audit: main.rs startup, session picker, slash fork/clone/import, apply_session_calls new/fork/switch; davinci_interactive.rs /new, fork/clone/import and Shell::resume; rpc.rs create/clone/fork/switch; sdk.rs session-path/continue/create. Core Agent::set_runtime clones into both contexts but retains an existing abort_signal; rebinding must preserve an externally supplied UI abort signal and refresh one owned by the old runtime. WorkflowExecutor retains a RuntimeHandle clone, so stale executors can retain the old journal lease.
- The preceding goal turn was progress (737fc64); this checkpoint adds required fresh-process evidence and a current-source integration audit. No full F03 task, requirement, or final program gate is marked complete. All fourteen plans remain within the active objective.

## Partial F01 project-save failure checkpoint

- Base revision: 50d1178. Native and RPC approval adapters no longer convert a failed project-rule save into an automatic session grant. Native keeps the original request/reply channel pending, shows the save failure, and reopens Once/Session/Deny choices. RPC asks one additional question without Project. A forged or stale unoffered Project answer denies. Successful persistence still acknowledges Project only after the write succeeds.
- RED: the existing RPC untrusted-project answer test failed when changed to require Deny. New save-failure adapter fixtures then failed on missing integration helpers. GREEN: permission selector passed 8 tests across both product suites (1,394 filtered). Fixtures force an actual filesystem failure using a regular file at .davinci, then cover explicit follow-up scopes, native Escape, RPC cancellation, forged Project and successful persistence. This is not a disk-full or physical terminal test.
- Product all-target Clippy with warnings denied passed. Independent review found no findings and reran six relevant tests; scoped diff check passed. Changes use the existing native Ask overlay and RPC extension select transport; upstream RPC source remains unchanged.
- This closes only the existing fallback defect within F01-T4. Policy-owned legal choices, challenge identity/revisions/expiry, narrow rule construction, concurrent settings updates, true disk-full, session-reload consent clearing and full host/platform interaction gates remain open. No full task or requirement is marked verified.

## Partial F01 post-callback validation checkpoint

- Base revision: ead20bc. The active turn gate snapshots permission policy before Ask, invokes the existing host callback without holding that lock, catches unwinding callback panics, and rejects cancellation or any changed policy before remembering consent. It reconstructs the approval request against current filesystem classification and requires the same request. Denial observations are emitted after releasing policy locks.
- RED: the new production-preparation regression failed because a callback could introduce a write deny and still return an accepted session grant. GREEN: the fixture covers deny insertion, mode change, cancellation, callback panic, unchanged Once and unchanged Session; only the two unchanged controls perform the fixture write. The related turn selector passed 6 tests / 424 filtered. Agent all-target Clippy and formatting passed. Independent review found no findings and reran the new regression and subscriber-refusal test.
- This is a post-callback check, not the final challenge/execution-ticket contract. Pending IDs, monotonic policy/contract revisions, digest/source binding, expiry, queue backpressure and a second dispatch-time ticket validation remain open. A policy mutation that changes back before comparison is not detected by this snapshot check. Blocking callback cancellation still requires the typed host lifecycle. No full F01 task or requirement is verified.

## Partial F01 settings-preservation checkpoint

- Base revision: aea1e80. remember_project_rule now initializes an empty document only for a missing or successfully read empty file. Any other read failure returns an error before mutation/temp-file writing, preserving unreadable existing settings. This also feeds the native/RPC explicit save-failed path from ead20bc.
- RED: an actual invalid-UTF-8 settings fixture failed because saving a rule incorrectly succeeded. GREEN: permissions::tests passed 8 tests across product suites (1,395 filtered), including byte preservation for .davinci and legacy .pi and the existing unrelated-key/deduplication checks. Product all-target Clippy with warnings denied and scoped rustfmt check passed. Independent review found no findings and reran all eight permission-settings tests.
- Remaining F01 work is unchanged: policy-owned scopes and typed challenges, revision/digest/source binding, immediate dispatch revalidation, lifecycle/transport cancellation, persistence durability/concurrent updates, and full native/RPC/platform acceptance. This partial correction does not mark F01-T4 or any complete requirement verified.

## Partial F01 policy-owned scope checkpoint

- Base revision: 097567e. New approval.rs defines policy-owned choices. Ask requests offer persistent grants only for exact representable ordinary file subjects or a single routine local shell subject; project grants additionally require host-resolved trust. Sensitive, external, compound-shell and unrepresentable subjects retain one-shot approval. Existing explicit deny precedence is preserved, and malformed file mutations fail closed.
- Native/RPC panels consume issued choices, preserve correct selection-to-decision mapping when rows are removed, and reject unoffered grants before saving. The agent turn gate independently rejects forged scopes. Approval summaries are bounded and remove terminal controls; the current four-decision host bridge does not yet implement deny-with-instructions editing.
- Validation on this source delta: full offline locked davinci-agent suite 435 passed; product permission selector 21 passed; agent and product all-target Clippy with warnings denied passed. Independent review caught ineffective compound-shell persistence; the new regression was observed RED, then GREEN after restricting persistence to the same parsed single subject. Reviewer reran f01_ (7 passed) and product permission (21 passed), then reported no actionable findings for this bounded checkpoint.
- F01-T1 remains in progress. Pending work includes typed challenge IDs/replies, policy/contract/source revisions, digest and expiry binding, dispatch-time consumption, modal-local instructions, lifecycle/transport cancellation, concurrent durable settings updates, exact rule presentation in RPC, and full physical/platform acceptance. F03 journal/session activation and every other expansion plan remain within the active objective. No full task or requirement is verified by this checkpoint.

## Partial F01 monotonic policy-revision checkpoint

- Base revision: 51cff52. Agent.permissions now uses PermissionState, the single existing policy lock boundary with a monotonic revision. Mutable guard access advances the revision even if a caller restores the original fields or replaces the complete policy value; reads leave it unchanged. Revision exhaustion becomes None and approval resolution rejects it rather than wrapping. Product construction and nested worker setup use the tracked state; embedders constructing the public field must use Arc::new(PermissionState::new(policy)) instead of Arc<Mutex<PermissionPolicy>>.
- The actual post-callback tool gate compares the issued/current revision under the policy lock before recording grants. Added mode-restored to the actual preparation fixture: observed RED with the prior value-only equality check, then GREEN after integration. Unit tests cover read stability, whole-value replacement and revision exhaustion.
- Validation: full offline locked agent suite 437 passed; product permission selector 21 passed; agent/product all-target Clippy with warnings denied and scoped formatting/diff checks passed. Typed challenge IDs, expiry, source/contract binding, dispatch-time consumption and all remaining F01/full-program gates are still open; no full task or requirement is verified.
- Independent review found no actionable findings and independently passed permission_state (3 selected, including the existing permission-state test) and the actual callback-change fixture (1 selected). This is a revision prerequisite, not completion of F01-T2.
- Workspace all-target offline locked cargo check passed (14 crates compiled), verifying in-tree consumers of the changed public permission-state type. Full workspace tests and final release gates remain outstanding.

## Partial F01 typed challenge lifecycle checkpoint

- Base revision: 658a643. approval.rs now owns versioned challenge/reply DTOs, the trusted typed responder, and an ephemeral pending table capped at 16 entries. Ten-minute expiry uses wall time plus Instant. Resolution removes the pending ID atomically on its first attempt, checks exact issued choice, digest and policy revision, and rejects stale/unknown/duplicate replies. Dropping the lease releases pending state on callback failure/cancellation. Full arguments, canonical tool name, canonical policy subject, cwd bytes, engine owner nonce and runtime run/actor/session identity use the existing tool-ledger canonical SHA-256 function.
- The actual turn permission gate issues after policy Ask and routes both typed responders and existing host callbacks through this lifecycle; it re-runs policy and revision checks before accepting a reply or recording a session rule. Typed responders take precedence, and host teardown/nested worker setup clears them alongside legacy callbacks. Bounded deny instructions are user guidance and cannot become a grant. Existing native/RPC continue through the compatibility bridge; no richer wire protocol is advertised.
- RED: the registered actual typed-reply fixture failed on the missing responder/reply/Agent interface. GREEN: matching reply reaches an actual local write; forged challenge ID never reaches execution. Pending-table fixtures cover identity/digest/revision/expiry/unoffered choice, duplicate resolution, invalid instructions, capacity rejection and lease release, plus complete-argument/runtime-identity digest changes.
- Validation: full offline locked agent suite 441 passed after fixing an exact denial-message regression (prior run 440 passed/1 failed); refreshed F01 selector 11 passed after canonical digest reuse; product permission selector 21 passed; agent/product all-target Clippy with warnings denied passed. Independent review found no findings in the bounded scope and independently ran F01 (11) and product permission (21).
- F01 remains incomplete: source and task-contract binding, immediate dispatch-time ticket, typed native/RPC wire and modal editor, callback interruption/EOF, session lifecycle, and durable settings freshness are open. Queue capacity currently rejects a new request explicitly; ordered backpressure is still required. Expiry rejects a returned late answer but cannot yet interrupt a blocked legacy callback. No full task or requirement is verified; all fourteen plans and final platform/workspace/release/install gates remain active.

## Partial F01 RPC disconnect and challenge deadline checkpoint

- Base revision: 03dbecf. The production RPC UI waiter distinguishes receiver timeout from EOF/disconnect, returns the existing denial default on disconnect, and restores parked commands in source order. Request output uses a scoped stdout lock and propagates write/flush failure to denial without an application retry loop. Checked deadline construction rejects overflow; matching cancelled replies deny and wrong-ID/malformed replies cannot resolve the current prompt.
- The actual RPC host now installs the typed ApprovalResponder and bounds each legacy-compatible select call to the challenge's remaining lifetime. It checks current policy revision, reconstructed Ask and wall/monotonic expiry before and after the reply, preventing known-stale project saves. The engine still independently consumes and validates the reply. ApprovalReply::from_legacy is public solely as the trusted host adapter; the existing wire format is unchanged.
- RED: the production-waiter EOF regression failed after waiting the full one-second deadline instead of terminating immediately. GREEN: three F01 RPC fixtures passed, covering EOF and queue preservation, matching/cancelled/wrong-ID/malformed replies, zero deadline, and current/expired/policy-ABA project persistence. This does not establish a real connected-client or native interaction test.
- Validation on this delta: product permission selector 21 passed; RPC selector 19 passed across both suites after an offline locked debug build. Its initial run failed because an existing security RPC subprocess test requires target/debug/davinci.exe; building that executable resolved the prerequisite and the rerun passed. Agent/product all-target offline locked Clippy with warnings denied, scoped rustfmt and diff checks passed.
- F01-T5 is in progress, not verified. Native/legacy callback interruption, immediate RPC abort handling during approvals, session lifecycle, typed capability negotiation, modal-local deny editing, print structured output and user docs remain open. Settings save still has a race between the freshness check and persistence; final dispatch tickets, source/contract binding and ordered queue backpressure also remain required. All fourteen plans and final workspace/platform/release/install gates remain active. The debug build was not installed.
- Independent review found no concrete findings in this bounded diff and reran agent F01 (11), product permission (21) and F01 RPC (3), all passing. It confirmed the pre-existing RPC new_session replacement loses the approval callback/responder, while clone/fork/switch retain it; host session rebinding must address this during the outstanding lifecycle integration.

## Partial F01 session-consent revocation checkpoint

- Base revision: e4fc90d. Agent::reset_session_approvals clears only ephemeral session grants, advances policy revision through the existing PermissionState guard, and revokes all outstanding leases in the shared approval registry. load_from_session calls this boundary before loading session messages. Host callbacks, configured deny/allow rules and permission mode remain intact. This intentionally clears consent even on same-session reload, as required by F01.
- RPC new_session now preserves the configured permission state and legacy/typed host callbacks while creating the fresh agent. It clears prior consent and pending challenges only after session creation succeeds, before the agent swap. The new agent owns a fresh pending registry. This fixes the previous policy/callback loss without claiming completion of the broader runtime/session rebinding work.
- RED: the RPC regression failed on permission-state identity after new_session; the core load regression failed because session consent survived. An initial test-authoring type mismatch was corrected before observing the RPC assertion failure and is not counted as RED. GREEN: both regressions passed, including pending lease rejection with the original revision/digest, revision change, preserved explicit deny/trust/mode and retained callbacks.
- Validation: full offline locked agent suite 442 passed; product rpc::tests 10 passed; agent/product all-target offline locked Clippy with warnings denied, scoped rustfmt and diff checks passed. Independent review found no findings, confirmed policy-then-registry lock ordering, and independently reran both regressions successfully.
- F01-T4/T5 remain in progress. Native/main direct session assignments must adopt the consent boundary as part of the owning host/session migration; F03 durable journal/runtime activation, immediate callback interruption, source/contract binding, dispatch tickets, atomic settings freshness and the remaining fourteen-plan integration/platform/release gates stay open. No full task or requirement is marked verified and no executable was installed.

## Partial F01 native, legacy and extension session boundary checkpoint

- Base revision: cc4af55. Native SlashAction::NewSession, legacy /new, and extension newSession now call the shared consent-revocation boundary after successful session creation and before changing the live session. They preserve existing UI and error behavior. Failed creation keeps the prior session and its consent; startup creation has no prior consent and remains unchanged.
- RED: the cross-host fixture failed because native /new retained its session grant. GREEN: the same fixture passed all six native/legacy/extension success and filesystem-failure cases, checking prior-session identity, consent, policy revision and mode. These are actual host adapter calls with temporary session roots, not physical UI interaction.
- Validation: cross-host regression 1 passed; existing apply_session_calls selector 1 passed; product permission selector 21 passed; product all-target offline locked Clippy with warnings denied, scoped formatting and diff checks passed. Independent review found no findings and independently reran the cross-host fixture successfully.
- This integrates consent reset at the identified direct new-session adapters, but does not complete F01 or F03 runtime/session activation. Next F01 host work includes replacing native reply_rx.recv with a typed expiry/cancellation-aware responder and ensuring UI error exits release a waiting scoped worker before join. Source/contract binding, final execution tickets, settings transaction freshness and all full-program gates remain open. No full task or requirement is verified and no executable was installed.

## Partial F01 native approval cancellation and expiry checkpoint

- Base revision: 286dfa0. The native turn host now installs the typed ApprovalResponder. NativeApproval carries the existing policy request, a private reply channel, shared liveness and wall/Instant deadlines. The worker waits in at most 25 ms slices, denying on abort, expiry or disconnect and marking the packet no longer live when it returns. The engine still validates the typed challenge reply and consumes its pending lease.
- The actual UI loop discards dead/expired requests and closes their Ask overlay, including expiry between key polling and reply handling. It checks packet liveness and abort before persistence. A scoped ApprovalUiExit guard sets abort on early UI error/panic before std::thread::scope joins the worker; normal completion disarms it. This releases a pending approval wait, not an arbitrary uninterruptible provider or extension operation.
- RED: the inline native lifecycle fixture failed on the missing wait/exit-guard contracts before implementation. GREEN: the fixture covers allowed reply, prior cancellation, prior expiry, disconnected receiver, UI-exit cancellation with the reply sender retained, and expiry while already waiting. These exercise the production wait helper and exit guard; physical terminal draw/poll failure injection remains unverified.
- Validation: 88 native adapter tests passed on the final source delta; product all-target offline locked Clippy with warnings denied and scoped rustfmt/diff checks passed. Clippy initially found four redundant borrows introduced by the packet refactor; these were fixed before the passing rerun. Independent review found no actionable findings and independently reran the focused lifecycle fixture successfully.
- F01 remains incomplete: native project persistence still needs policy/source/contract freshness and an atomic commit boundary; modal-local deny instructions, paste/voice isolation, legacy interruption, immediate RPC abort handling, source/contract binding, final dispatch tickets and ordered queue backpressure remain open. All fourteen plans, F03 durable session activation, full workspace/platform/physical interaction/release/install gates remain active. No full task or requirement is marked verified and no executable was installed.

## Partial F01 modal focus and draft isolation checkpoint

- Base revision: 521adb4. Permission Ask overlays now number choices and accept plain 1-5 presses solely to focus offered rows. Approval requires a plain Enter press even if tui.select.confirm is remapped. Other Ask overlays retain existing confirmation bindings. The existing bounded Ask rendering and composer paste/dictation gates are reused.
- RED: the actual native approval-key fixture first failed because numeric input left focus unchanged, then failed because remapping confirm to x granted session approval. GREEN: the fixture now passes at 40x12 and 120x40, exercising mode cycling, Ctrl+T, slash, numeric focus, remapped confirmation, paste, dictation insertion rejection, Enter and Escape while preserving a Unicode draft and cursor. These are host/model calls, not physical terminal or microphone acceptance.
- Validation: offline locked TUI davinci:: selector passed 358 tests with one existing ignored manual mockup audit and 219 filtered; native adapter selector passed 89 tests with 1,323 filtered; TUI/product all-target Clippy with warnings denied passed. A final test-only Escape assertion refinement was followed by a passing focused modal test and scoped rustfmt check. Independent review found no findings and independently passed the native modal fixture and permission-rendering hint test.
- F01-T3 is in progress. Modal-local deny instructions and typed reply emission, complete risk/source rendering, physical resize/input/voice acceptance, native persistence freshness, source/contract binding, final execution tickets and ordered queue backpressure remain open. F03 durable runtime/session activation and every other expansion plan remain active. No whole task or requirement is verified; no release executable was built or installed in this checkpoint.

## Partial F01 native denial-instructions checkpoint

- Base revision: 8392ea0. The existing permission Ask adds deny with instructions only when that exact choice is policy-issued. Explicit Enter opens a separate modal-local Composer; non-empty text requires another plain Enter press to confirm denial. Esc and Ctrl+C discard instructions and deny; Ctrl+C also aborts. Input is bounded to 4,096 UTF-8 bytes. Paste/dictation remain trapped. The normal conversation draft is never moved or replaced. A grapheme/column-bounded horizontal view keeps the editor caret visible for long input.
- The actual native channel now carries NativeApprovalAnswer through the existing expiry/cancellation wait, preserving instructions when converted to the typed ApprovalReply. The engine consumes and validates that reply and returns a denied tool result with guidance, without dispatching the executor or recording a grant. Expiry, cancellation, new Ask and normal turn completion clear modal text. Permission traces no longer log key contents or instruction payloads.
- RED: policy-owned offer fixture observed the missing fifth choice; a long Unicode rendering fixture observed excessive panel height; the actual engine fixture observed loss of guidance past 1,024 bytes. GREEN: the native offer/editor/channel fixtures pass, including empty/repeated Enter, Unicode draft/caret preservation, resize, paste/dictation rejection, UTF-8 bounds, Escape, Ctrl+C and timeout/disconnect behavior. Engine sanitization now preserves accepted guidance up to 4,096 bytes while display summaries keep the prior 1,024-byte limit. A temporary renderer u16/usize mismatch was corrected before its passing rerun. An existing save-failure fixture was updated for the new offered denial row after its expected-count failure.
- Validation: full offline locked agent suite 443 passed; native adapter selector 92 passed (1,323 filtered); TUI davinci:: selector 359 passed with one existing ignored manual audit (219 filtered). Agent F01 selector 13 passed. Scoped formatting and diff checks passed. All these are offline host/model/channel and engine fixtures, not a physical terminal/microphone or live provider acceptance test.
- Final agent/TUI/product all-target offline locked Clippy with warnings denied passed. Independent review found no findings in the complete checkpoint and independently passed the actual engine guidance fixture, approval module (8 tests), long Unicode renderer fixture and native editor/bounds/channel fixtures.
- F01 remains in progress: full risk/source rendering, legacy/RPC denial-instructions adapters, print structured approval output, source/contract freshness, final dispatch tickets, atomic settings persistence, queue backpressure and full cross-host/platform acceptance remain required. F03 durable session activation and all other expansion plans remain active. No whole task or requirement is verified; no executable was installed and no external delivery occurred.

## Partial F01 RPC denial-instructions checkpoint

- Base revision: c020d50. The production typed RPC approval adapter adds deny with instructions only when that exact ID/scope is present in both the policy request and issued challenge. It reuses existing select/input/confirm wire operations, verified against the upstream reference and current RPC serializer. Non-empty input of at most 4,096 UTF-8 bytes requires an affirmative boolean confirmation before becoming a typed denial reply. No new wire fields or client capability claims are introduced.
- Each dialog uses the same remaining challenge lifetime and pre/post policy freshness checks as the initial select. Cancelled, empty, oversized or malformed answers and policy changes deny without instructions. The existing project-save retry passes through the same dialog wrapper; legal grant and persistence handling remain in the existing resolver. Native/RPC usage is documented in docs/permission-modes.md, separately from its historical installed-build record.
- RED: the actual RPC adapter fixture failed because the instructions option was missing. GREEN: eight cases cover accepted instructions, input cancellation, empty/oversized text, rejected/malformed confirmation, policy mutation during confirmation and an unoffered forged answer. Each emitted call passes through the current extension-UI serializer and carries a bounded timeout. These are synchronous host/serialization fixtures, not connected-client or provider acceptance.
- Validation: new regression 1 passed; product F01 RPC selector 5 passed (1,411 filtered); product permission selector 21 passed (1,395 filtered); product all-target offline locked Clippy with warnings denied passed. F01-T5 and the full expansion remain in progress. Legacy denial instructions, immediate RPC abort handling, structured print output, source/contract freshness, atomic settings updates, dispatch tickets and full cross-host/platform/workspace/release/install gates remain open. No whole task or requirement is marked verified; no executable was installed.
- Scoped rustfmt and diff checks passed. Independent review found no findings in source or docs and independently passed the new denial fixture, stale challenge/save fixture, explicit save-failure retry, unoffered scope fixture and all five F01 RPC tests.


## Partial F01 RPC dialog cancellation checkpoint

- Base revision: a3ecc62. Active synchronous RPC turns install a fresh UI cancellation signal and restore the prior agent signal afterward. The installed UI waiter intercepts a typed abort only during that scope, rejects the pending dialog immediately, signals the actual agent, emits one normal abort acknowledgement, and consumes the abort rather than replaying it against a queued prompt. Other queued commands retain their order; later approval responses are not consumed by the cancelled wait. Already cancelled turns cannot reopen another dialog. Idle extension dialogs retain their existing command handling.
- RED: abort followed by a late allow-once response incorrectly returned the approval. GREEN: the regression now returns denial and retains the later response. Actual emission/wait and agent-loop fixtures cover cancellation before a provider call, queued-command preservation, repeated cancelled dialogs, fresh signals across two turns, previous signal restoration and idle command handling. Runtime-owned cancellation tokens and session replacement logic are unchanged.
- Validation: offline locked product F01 RPC selector 8 passed (1,411 filtered); permission selector 21 passed (1,398 filtered). These are in-process offline fixtures, not connected-client, streaming-provider or platform acceptance. Abort outside an active dialog remains outside this checkpoint; the synchronous RPC reader still does not interrupt a provider call.
- F01 and the complete expansion remain in progress. Source/contract freshness, dispatch tickets, atomic settings persistence, queue backpressure, legacy instructions/cancellation, structured print output, full workspace/platform/release/install gates and other plans remain required. No whole task or requirement is marked verified; no executable was installed and no external delivery occurred.
- Final scoped rustfmt and diff checks passed; product all-target offline locked Clippy with warnings denied passed. Pre-existing cancellation remains effective when the turn scope starts.
- Independent review found no blocking findings and independently passed all eight F01 RPC tests plus the source diff check. Exact abort acknowledgement stdout capture remains a minor unverified assertion; the actual emission path ran in fixtures.


## Partial F01 structured print approval checkpoint

- Base revision: a1b0b8f. The actual print adapter now installs a turn-scoped typed responder. The first engine-issued unresolved challenge stops further model calls and later supplied prompts, returns one approval_required JSON object with call ID, action, redacted target, permission mode and resolved global settings path, and exits 1. JSON mode retains its existing JSON event stream before this object. No consent/settings are written; existing host callbacks and cancellation state are restored. Already-authorized calls retain normal execution/exit behavior. Help and permission docs describe the new output.
- RED: the named F01 noninteractive contract first failed on the missing host adapter. Its next run exposed dropped terminal tool results when cancellation occurred during preparation. The ordinary scheduler adapter now retains immediate denials, emits source-ordered terminal results for skipped calls without invoking executors, and releases only reservations owned by Ready preparations. An already-set host cancellation signal stops scheduling even with an attached uncancelled runtime token. The engine fixture covers both runtime configurations with calls before and after the denied operation. Mixed propose_plan batch cancellation and broader asynchronous cancellation still require their own integration audit.
- Review requested a target in the report and a process/output regression. The target assertion failed before the field was added. The new subprocess fixture also failed against the earlier debug build lacking that field; after rebuilding it passed all four cases: plain Manual, JSON Manual, JSON Always Approve, and legacy PI_CODING_AGENT_DIR override. It checks EOF termination, every stdout line, exit code, target/config path, absent unauthorized file/settings writes, and no later supplied prompt after unresolved approval. It is explicitly ignored in ordinary cargo test because it requires a freshly built product, and was explicitly selected with DAVINCI_PRINT_TEST_EXECUTABLE and --ignored; it never silently passes when the binary is missing.
- Validation: offline locked agent suite 444 passed; product F01 selector 15 passed before the subprocess fixture addition; final targeted noninteractive fixture 1 passed. The explicit process selector passed 1 test containing four real child-process cases (1,420 filtered). Agent/product all-target offline locked Clippy with warnings denied passed. Debug cargo build passed. Scoped rustfmt and diff checks passed. No live provider or network backend was used.
- Verified debug binary: target/debug/davinci.exe SHA-256 feb23219c86668912c7a308537208ef161cf14e2477469ed5f0ca8828af0dedc. Source SHA-256: turn.rs e3e23682b1f41853b963ba77ef8c95202ce2365288b4dcd741bb6d06f9d1d281; main.rs b87a1d34e3f0db17432cdb33a39855c46f02788f6e80176e58fbf5241b045514; help.txt e96e6b3ec0eba2a754929a0c0f0e0b221ecac495789c24f195f146a56723fecb. This is a worktree debug build, not a release or installed executable.
- F01 and the complete expansion remain in progress: source/contract freshness, dispatch tickets, atomic settings persistence, queue backpressure, full legacy parity, cross-feature/platform/workspace/release/install gates and the other plans remain required. No whole task or requirement is marked verified. Unrelated user instructions remain unstaged; nothing was installed or externally delivered.
- Final full product suite: 1,418 passed, 3 ignored. The required ignored subprocess fixture was explicitly run and passed separately against the rebuilt debug executable. Independent final review found no material findings and independently passed the subprocess regression and cancellation fixture.

## Partial F01 mixed-plan cancellation checkpoint

- Base revision: 884b645. Mixed propose_plan batches now emit source-ordered terminal errors for every skipped call after cancellation, without dispatching skipped executors or mutating the Living Plan. This closes the ordinary-versus-mixed batch result gap recorded above.
- RED: the mixed-batch regression returned only the first denied result instead of all three calls. GREEN: cancellation during approval and pre-cancelled batches both return every result; skipped calls emit no execution starts, create no files, and preserve plan state.
- Independent review found that post-tool hooks could turn cancellation into apparent success. A second regression reproduced is_error=false. The shared finalizer now preserves engine denial/cancellation markers and error state across hook decoration, covering ordinary and mixed batches. Tests include a hook attempting that rewrite.
- Validation: offline locked full agent suite 446 passed; final F01 selector 16 passed (430 filtered). Agent all-target Clippy with warnings denied passed; scoped rustfmt and diff checks passed. Independent review found no remaining findings and independently passed both new regressions. These are offline engine fixtures, not live host/platform acceptance.
- The expansion remains in progress. Remaining F01 gates include cross-feature source/contract freshness, dispatch tickets, atomic settings persistence, queue backpressure, legacy parity and asynchronous provider cancellation. Other plans and final workspace/platform/release/install gates remain incomplete. No executable installed or external delivery performed.
- Final product suite: 1,418 passed, 3 ignored. The pre-existing fresh-executable subprocess gate was not rerun in this checkpoint; no new executable build or install is claimed. Final agent all-target Clippy passed after the hook integration assertion.

## Partial F03 runtime rebinding prerequisite

- Base revision: 0866584. Agent::set_runtime now refreshes an abort signal owned by the previous runtime, using Arc identity rather than its Boolean value, while retaining independently supplied host/UI signals. Agent::abort_requested observes the active runtime as well as local and UI cancellation. Both Agent.runtime and ToolContext.runtime continue to receive the new binding.
- RED: runtime replacement retained the cancelled old runtime flag. After that fix, the same fixture exposed hidden active-runtime cancellation when an external UI signal existed. GREEN: both configurations pass; final assertions also cover UI cancellation and rebinding an already-cancelled same runtime without clearing it.
- Validation: offline locked full agent suite 447 passed; final focused fixture 1 passed (446 filtered); product F01 selector 15 passed, 1 ignored (1,405 filtered). Agent all-target Clippy with warnings denied passed; scoped rustfmt and diff checks passed. These are core/host fixtures, not durable session activation or platform acceptance.
- Still required: fallible atomic host session preparation, journal lineage/lease selection, observer and WorkflowExecutor rebinding, all session assignment callers, ownership fencing, and old-runtime job callback lifecycle. The latter still attaches cancellation to a shared JobBook; this checkpoint only resolves the agent abort-signal ownership. Do not activate durable tasks until the full migration is safe. Full expansion, workspace/release/install gates remain incomplete; no installation or external delivery.
- Review identified inconsistent scheduler consumers: nested batches observed only the UI signal, while top-level batches preferred the runtime token. A nested-batch regression reproduced a second write reporting success after runtime cancellation. Both consumers now use the same live abort_requested predicate through a crate-private scheduler entry point; the public AtomicBool API remains compatible.
- An additional assertion reproduced a leaked pending ledger reservation for a skipped nested operation. Nested batches now release only their skipped Ready-owned reservations, preserving Wait ownership. The fixture covers both runtime and UI cancellation, first-file success, absent second file, explicit skipped status and no pending/executing records.
- Final engine validation after scheduler integration: full offline locked agent suite 448 passed. Clippy initially flagged a needless wrapper lifetime; it was removed and all-target Clippy with warnings denied passed. Durable host activation remains unimplemented; the audit confirms final session/registry binding must precede WorkflowExecutor construction.
- Final product checks after scheduler integration: F01 selector 15 passed, 1 ignored; full suite 1,418 passed, 3 ignored. Fresh-executable subprocess acceptance was not rerun and no executable was built or installed. Final scoped formatting and diff checks passed.
- Independent final review found no blocking findings and independently passed both new fixtures. Adjacent cancellation gate remains: already-running built-in tools still receive one raw abort flag, preferring runtime over external UI. This checkpoint proves skipping future scheduler groups, not interrupting already-running tools or providers.

## Partial F03 runtime job-cancellation ownership

- Base revision: 3e40ebf. Agent::set_runtime binds the shared JobBook to the current cancellation-token identity. The owner comparison and kill operation run under the same JobBook mutex; callbacks from replaced runtime clones cannot kill replacement jobs. Weak callback captures prevent stale runtimes from retaining the book, and repeated same-token binding does not append another pending callback. The existing public attach_job_book API remains unchanged.
- RED: a real isolated local stdin-waiting child was killed by the old runtime after rebinding. GREEN: the same fixture remains Running after old cancellation and becomes Killed after active cancellation. No network/provider was used. Windows child-process execution is verified; Unix branch is present but not executed here.
- Callback registration now checks cancellation while holding the callback-list mutex, preventing insertion after cancel has drained the list; callbacks still execute outside the mutex. Added a 64-iteration concurrent registration/cancellation check and a bounded-registration/weak-retention fixture. The race check is supplementary concurrency evidence, not exhaustive scheduling proof.
- Validation: offline locked full agent suite 451 passed; agent all-target Clippy with warnings denied passed; scoped formatting and diff checks passed. Full durable host activation, per-job/task ownership, resume fencing, source/contract integration, running-tool/provider cancellation and all remaining expansion/workspace/platform/release/install gates remain incomplete. No executable built or installed and no external delivery.
- Independent review found the spawn-to-registration gap: cancellation could finish before a child entered the registry. The extended real-child fixture reproduced Running instead of Killed after late registration. JobBook::register now checks the current owner flag after insertion under the same registry lock and kills the new job when cancelled. Final full agent suite 451 passed and all-target Clippy passed after this correction.
- Final product suite after the registration correction: 1,418 passed, 3 ignored. Independent final review found no findings and independently passed all three new fixtures. The child-process fixture was strengthened to poll actual process exit (bounded to five seconds), and its final focused run passed; registry Killed status alone is not the process-exit evidence. Final all-target Clippy and diff/format checks passed.

## Partial F03 workflow session binding

- Base revision: a3580f6. The product host now constructs WorkflowExecutor after binding the session and replaying existing observer projections. The extracted runtime_host::configure_session_workflow helper is called by complete_prompt_with_host, so the executor captures the final session identity instead of the earlier unbound runtime clone. Existing best-effort observer log handling remains unchanged; this is not the fallible durable loader.
- RED: the actual host helper regression observed executor session_id=None while the host had custom-session-id. GREEN: both IDs agree and an event emitted through the executor reaches the actual session runtime sidecar with that ID. The custom ID fixture deliberately avoids assuming session IDs are UUIDs.
- Validation: focused new regression 2 passed across the library and binary suites; runtime_host::tests 22 passed (1,401 filtered). Product all-target offline locked Clippy with warnings denied, scoped rustfmt and diff checks passed. No full product suite or fresh executable acceptance was rerun for this narrow host-order change.
- Audit correction: this assembly occurs on every prompt inside complete_prompt_with_host, not only at startup. Print multi-prompt, RPC and native interactive callers all rebuild the runtime. Durable activation must reuse session-owned task lineage/registry across turns or safely retire every previous lease owner before reopening. Prompt-local RunId::new() is not stable task lineage. Observer replay also does not advance emitter sequence counters, and whole-runtime replay is incompatible with an authoritative task journal. These prerequisites remain open alongside fallible atomic session switching, worker fencing and all remaining expansion/platform/workspace/release/install gates. No whole task is marked verified; no executable was built or installed.
- Independent review found no findings and independently passed the new regression in both library and binary targets. The review covers the bounded session-binding checkpoint, not durable activation or session recovery.

## Partial F03 same-session turn ownership

- Base revision: 59c8b84. Agent::set_runtime captures the session file path, header ID and runtime lineage; runtime_for_session validates all three against current state before reuse. An explicit load_from_session invalidates that binding, including same-file reloads. The shared production complete_prompt_with_host path now supplies this validated previous runtime to configure_session_workflow.
- Ordinary same-session prompts retain the run/main-agent identities, task commit coordinator, live agent registry, mailbox queues/deduplication and shared event counters. New turns create fresh cancellation tokens, hooks and workflow executors. The session bus is shared with existing worker clones; its turn observers are atomically refreshed while session observers retain their identity. The log writer is installed once per session binding. Crash replay is skipped for this same-process path, preventing task replacement, false worker termination and redelivery of drained messages. Different session files cannot reuse the binding merely by sharing a header ID.
- RED: the host fixture observed different run IDs across prompts; the agent fixture exposed a different file reusing an ID-only binding. An extended host assertion observed runtime sequences [1, 1] instead of [1, 2]. Independent review identified same-process crash replay of running agents; a real registry/task fixture reproduced Failed instead of Running. All four issues were corrected, preserving main-agent identity along with task ownership.
- Validation: initial combined F03 selector 80 passed. Full offline locked agent/product suites 1,876 passed, 3 existing ignored (five suites), after the background-observer correction. The same-session host test runs both ephemeral and injected real durable registries, proves shared state through old/new handles, second-writer exclusion, fresh cancellation, mailbox drain visibility, workflow/tool-context lineage, monotonic runtime-origin sequences and one log append per emitted event. This proves live in-process reuse of an existing lease; the product does not yet select/open durable journals automatically.
- Independent review found that old background worker clones could update shared state through stale observers and a separate log writer. The actual Agent::run_loop background agent tool fixture, with an injected runner held across the next host runtime installation, reproduced zero current-observer completions instead of one. Shared-bus turn refresh fixes this; the fixture verifies exactly one completion through current observers and the session log. A supplemental decision-bus fixture covers repeated refresh without duplicate session observers and the updated denial gate through an old worker handle. Subscriber destruction occurs outside the bus lock.
- Still required: persisted lineage selection across process restart, fallible atomic session activation and every switching caller, durable journal activation, legacy task migration, worker ownership fencing and transport, branch-specific task scope, surfaced persistence/replay failures, and the remaining expansion/platform/workspace/release/install gates. Explicit reloads still use the existing reconciliation behavior. No whole task is marked verified, no fresh executable was built or installed, and no external delivery occurred. Headroom was rechecked and is unreachable with zero reported savings; RTK was used throughout.
- Final combined F03 selector: 83 passed (1,797 filtered), including the supplemental refresh/denial test added after the full suite. Final agent/product all-target offline locked Clippy with warnings denied and scoped formatting/diff checks passed. Independent final review found no findings and independently passed both host fixtures in library/binary targets and the bus refresh test.

## Partial F03 fallible runtime preparation

- Base revision: 2866a32. configure_session_workflow now returns read, replay, non-file-path, inspection and observer-open failures instead of silently installing an empty candidate. Same-process reuse keeps the existing session observer and skips crash replay. The shared prompt path returns a visible recovery-required error before set_runtime, emits terminal host lifecycle events, and starts provider lifecycle events only after preparation succeeds. This preserves the previous runtime handle on preparation failure; it does not yet make earlier session switching or all host setup atomic.
- RED: the host replay fixture accepted two corrupt runtime-log records; the prompt integration fixture exposed missing AgentSettled. A further lifecycle assertion exposed provider-start events despite failed recovery. The real fresh-executable print fixture returned exit code 0. Each intended failure was reproduced before its correction.
- Print and JSON print now classify recovery-required replies as fatal and stop queued prompts. The explicitly run child-process fixture uses isolated configuration/session roots, offline tool fixtures and null stdin; both modes exit 1, preserve corrupt bytes, do not dispatch the fixture write, and do not append the second prompt. The normal test suite ignores this fixture because it requires a separately built executable; it was explicitly executed successfully after the final offline locked debug build.
- Validation: initial product F03 selector 17 passed (1,413 filtered); final full offline locked product suite 1,427 passed, 4 ignored (three suites). The new executable fixture separately passed 1 test (1,430 filtered across two suites). Final product all-target offline locked Clippy with warnings denied, scoped rustfmt check and git diff check passed.
- Tested debug executable: C:/Users/sergi/Desktop/pi-rust-feature-expansion/target/debug/davinci.exe; SHA-256 89e8bfcb97a2447db6acd4bddda570ada3b744529a9caf50b86c8ab875ee0269. This is a local debug test artifact, not an installed release.
- Still required: persisted run-lineage selection, legacy multi-run task migration, authoritative journal activation, atomic session switching across all callers, worker fencing/transport, strict authoritative recovery semantics and the remaining expansion/platform/workspace/release/install gates. Existing observer-reader tolerance of a malformed final record remains unchanged. No whole task is verified. Headroom live statistics still report an unreachable proxy and zero savings; RTK was used throughout.
- Independent final review found no findings and independently passed both host/prompt regressions plus the fresh-executable plain/JSON process fixture.

## Partial F03 atomic observer-session loading

- Base revision: 3b3dd07. Agent::load_from_session now returns Result and validates/replays the target observer log into an isolated candidate before replacing messages, pending prompt state, session consent, session identity or runtime. Failed read/replay preserves the current session and its private host binding. Successful loads use set_runtime so ToolContext and owned cancellation references agree, without mutating registry/mailbox/task projections held by previous worker clones. Host reconciliation remains required before the next prompt.
- Updated all repository callers: RPC clone/fork/switch and SDK resume propagate errors; native and legacy interactive paths show errors before transcript/composer replacement. Extension/session-call application returns failed call indexes and messages; post-turn host processing filters those calls from success notes and rebinding and delivers a recovery reply plus terminal event through the saved sink after releasing the host mutex. Native hosted calls also exclude failed switches from success notes and preserve their error display across a successful sibling switch.
- RED: a corrupt-log core fixture changed the active session path despite failed replay. GREEN verifies unchanged session path/messages/runtime binding and intact corrupt bytes. Additional fixtures prove old worker task handles remain isolated on successful switching, current ToolContext points at the new session, old cancellation does not cancel the new session, actual RPC switch returns a recovery error, and the actual post-turn complete_prompt_with_host extension-switch path delivers the recovery reply/events/live sink while preserving the current session.
- Validation: initial combined F03 selection 89 passed, 1 ignored (1,797 filtered); final full offline locked agent/product tests 1,884 passed, 4 ignored (five suites). Final all-target agent/product offline locked Clippy with warnings denied and scoped formatting/diff checks passed. Fresh offline locked debug build succeeded; the existing plain/JSON print recovery child-process fixture was explicitly rerun and passed 1 test (1,432 filtered). Debug SHA-256: 39fb764bc22c3a2d7ee73552eb768e5b8b93c2351fa6b4326d188d5131eea067. No release was installed.
- This is atomicity for observer-backed Agent::load_from_session, not full durable activation. Still required: select/persist authorized lineage before task queries; migrate legacy multi-run records without replacing original run IDs; open authoritative journals; reconcile durable orphaned owners; bind non-task observer projections separately; migrate direct session assignments/new-session paths; and complete every remaining expansion/platform/workspace/release/install gate. TaskJournal currently requires an externally supplied RunId and rejects other lineages; observer replay into durable registries is intentionally forbidden. In-process subagents have no session, and graph workers launch with --no-session; parent coordinator transport and explicit worker fencing remain required. No whole task is marked verified.
- Review-driven additional RED/GREEN: a follow-on sendUserMessage after a failed switch entered the old session; a missing session file produced a normal reply; and final-event counting observed two non-retry AgentEnd events. Session create/fork/open/load now share the indexed failure path and stop the rest of the batch. Native turn triggering is suppressed after failure. The live sink defers the non-retry terminal event until post-turn session actions settle, and the returned terminal event is replaced on failure, yielding exactly one final outcome. Final product suite after these changes: 1,429 passed, 4 ignored; Clippy, scoped formatting/diff checks, fresh debug build and the explicit plain/JSON recovery process fixture passed again. Earlier combined count covers the unchanged agent implementation; the final product rerun covers the subsequent host changes.
- Independent final review found no findings and independently passed the four new core/RPC/post-turn regressions, workspace formatting check and scoped diff check.

## Partial F03 session-bound journal identity

- Base revision: 8877d7f. TaskRegistry::open_session_durable selects and returns a saved RunId under the existing exclusive journal lease. A versioned, checksummed session-key header is synced with the journal before successful return, including when no tasks exist. Commit frames and the legacy explicit-RunId open_durable API remain unchanged. The new opener requires its header; the legacy opener rejects the new format. No automatic legacy conversion or original task RunId rewriting occurs.
- RED: reopening an empty journal generated a different RunId. GREEN: stable empty lineage, same-file lease exclusion, wrong-session rejection, committed task replay, task-tail quarantine preserving the header, and no-write rejection of torn/unterminated/unknown-schema/checksum-invalid/oversized/legacy headers. A three-process fixture initializes empty identity, resumes it to create a task, and resumes again to query that same lineage; each child exits without destructors.
- Validation: all 18 journal tests passed; full offline locked davinci-agent suite 459 passed across two suites. Offline locked agent/product all-target Clippy with warnings denied, workspace formatting and diff checks passed. This validates storage APIs on Windows, not integrated session resume or Unix durability. Headroom remains unreachable with zero measured savings; RTK was used.
- Still required: host-owned canonical session key construction and activation, legacy multi-run migration with authorized mapping preserving original RunIds, reconciliation of orphaned owners and non-task observer projections, worker coordinator transport/fencing, task-board integration, and all remaining expansion/platform/workspace/release/install gates. F01/F03 remain in progress; no whole task is verified. No release was built or installed in this checkpoint.
- Independent review found no findings; reviewer independently passed all four session-journal tests, the existing killed-writer recovery fixture, and scoped diff checks.

## Partial F03 host authoritative journal activation

- Base revision: b9ec125. configure_session_workflow now binds the canonical session path and header ID to its tasks.jsonl sidecar, opens the authoritative registry and selects its saved RunId before constructing the workflow executor. Agent/mailbox observer projections replay separately. Same-process validated session reuse retains the existing registry and writer lease. Cold resume durably fails orphaned Running tasks with process_terminated and cascades dependency blocks before admitting commands; later resume does not repeat that transition.
- RED: the host resume fixture selected a fresh RunId, then its expanded orphan assertion observed Running instead of Failed. GREEN verifies saved identity, exact cancellation recovery, durable orphan failure/dependency blocking, repeated-resume idempotence and workflow identity. The former manually injected durable-registry fixture now exercises automatic host activation and the actual session journal lease. Session-bound child handles are rejected; sessionless workers remain ephemeral. Explicit parent transport is still required.
- Legacy task observer events with no initialized journal currently return a visible migration-required error without changing observer bytes or initializing a replacement journal. This is an unfinished migration gate, not completed legacy compatibility. Proposal-only TaskCompletionRequested events do not represent committed state and do not trigger that gate.
- Validation: full offline locked product suite 1,435 passed, 4 ignored across three suites before adding the supplemental ignored process test. Final product all-target offline locked Clippy with warnings denied and workspace formatting passed; scoped diff checks passed. Fresh debug product build passed. Explicit f03_print_ ignored-test selection passed 2 tests (1,438 filtered, two suites): plain/JSON recovery failure plus two real product processes creating then listing the same task. The latter explicitly enables experimental team tools, selects task_create/task_list and supplies a durable operation ID; it asserts successful tool-end events and exact journal record/run identity. Initial fixture failures from missing tool activation/operation ID, and an assertion expecting the title in the create response text rather than its details, were fixture setup corrections, not production RED evidence.
- Debug executable: C:/Users/sergi/Desktop/pi-rust-feature-expansion/target/debug/davinci.exe; SHA-256 4b853e78e373c542b6ffb8a9f7f2f077340a8ca237f9b9cc6e57ffa5e1747d93. Not an installed release. Independent review found no remaining findings after correcting the process fixture and independently passed host/reuse/background/process tests.
- Next: implement legacy multi-run task migration with a host-authorized saved mapping preserving original TaskRecord.run_id. Audit identified raw-run checks in tasks.rs initial_task_state, TaskOwner, claim_inner, list/get_ready; tools_task.rs get/list/update; and task_store.rs apply_changes, receipt validation and commit_inner. A record-carried mapping alone must not grant authority; validate against persisted host binding and bump enclosing schema so older readers fail closed. Also finish pre-prompt core/session switching activation, worker coordinator transport/fencing, task board and remaining expansion/platform/workspace/release/install gates. F01/F03 remain in progress; no whole task is verified. Headroom proxy remained unreachable with zero savings; RTK was used.

## Partial F03 migration snapshot and persisted access scope

- Base revision: 6a1a020. Added a version-2 session journal header containing a checksummed initial task snapshot. The registry/journal derive the authorized historical runs from that snapshot; each TaskRecord retains its original run ID, revision, timestamps and other metadata. Version-1 session headers and legacy explicit-run journals retain their existing APIs. Existing journals ignore new import input. Initialization uses the existing exclusive writer lease and sync-before-return path; invalid input is rejected before header bytes are written.
- The selected primary run can query and update historical tasks. Historical-origin queries remain limited to that origin; unrelated runs remain excluded. Dependencies may connect records within the persisted session scope. New tasks use the primary run, receipt requests remain bound to it, and replay rejects changes to original provenance. task_get now uses the same scope as list/claim/owned updates.
- RED: initial migration API returned an empty registry instead of the two supplied historical tasks. GREEN covers exact multi-run record retention, fresh registry reopen, revision advance, receipt replay after later completion, dependency release and new dependent creation, ignoring repeated import input, scoped reads/claims, and unchanged journal bytes on receipt retry. Invalid snapshots and damaged/schema-inconsistent headers, cycles, duplicates and changed origin fail closed. Independent review found a Ready-task dependency bypass; a new checksummed-header regression failed before the fix and passed after Ready snapshots were required to have completed dependencies. Running and terminal history remain explicit host-reconciliation inputs.
- Validation: final offline locked agent suite 465 passed across two suites; product suite 1,435 passed, 5 ignored across three suites. Agent/product all-target offline locked Clippy with warnings denied, workspace formatting and scoped diff checks passed. Independent final review found no material findings and passed all five migration tests plus scoped diff checks. This is registry/journal and adapter-unit proof on Windows, not completed legacy host migration, executable process acceptance of migration, or Unix validation. The snapshot currently shares the 1 MiB journal-header capacity; oversized imports fail before writing. Journal compaction/scaling acceptance remains open.
- Next integration: validate legacy observer provenance and transitions before building the initial snapshot, then replace the migration-required host gate. Audit confirmed task producers legitimately emit session_id None and observer sequences are not globally monotonic; allow missing session IDs only through the selected session-owned log and reject an explicit foreign session. TaskCreated record IDs/runs must match their envelopes, missing assignment/completion targets and divergent duplicates must fail. Legacy record-less creation and lossy completion events need an explicit compatibility treatment without inventing evidence, result text or owner consent. Preserve Running-to-Failed/process_terminated and dependency blocking before command admission.
- F03-T5 is now explicitly in progress alongside F01 and F03-T1–4. No whole task or requirement is verified. Live board/plan linkage, session-switch activation, coordinator transport/fencing, cross-feature integration, all other plans and final workspace/platform/release/install gates remain incomplete. No release built or installed. RTK used; Headroom status confirmed unreachable proxy and zero measured savings.

## Partial F03 validated legacy observer import

- Base revision: f2bcd44. New runtime/task_migration.rs owns TaskRegistry::legacy_snapshot. It validates task event schemas, explicit session IDs, embedded task IDs/runs, exact versus divergent duplicate events/creations, creation-before-mutation ordering, terminal history and dependency graphs. Missing session IDs are compatible with actual TaskRegistry producers; mixed observer sequences are not treated as a global clock. Topological reconciliation processes each task once after its prerequisites; terminal history and Running records retain their recorded states until host reconciliation.
- Successful completion must follow completed prerequisites in observer file order. A Pending creation may become ready through an earlier prerequisite completion without an explicit ready event. Existing historical metadata is preserved; the importer does not manufacture absent results, evidence receipts, revision/generation history or consent. TaskCreated without its optional record remains a visible migration/recovery error before any journal initialization. This unresolved legacy compatibility case is not marked complete.
- configure_session_workflow now validates recordful legacy history before initializing its version-2 journal snapshot. Reopening an initialized journal skips observer task import. The existing durable orphan reconciliation commits Running-to-Failed/process_terminated and dependent blocking before commands; original JSONL observer bytes remain unchanged. The original record-less-history no-write regression remains passing.
- RED/GREEN: the snapshot test first observed zero restored records, and the host test first failed at the prior migration-required gate. Both passed after implementation. A further premature-completion regression failed before readiness validation and passed afterward. Four snapshot tests cover multi-run metadata, ownership observations without new consent, strict provenance, duplicate replay, missing metadata, cycles, dependency cascades and ordered completion. The host migration regression executes twice across the product lib/bin test targets and verifies import-once, saved run identity, exact orphan revision/result recovery and unchanged legacy log bytes.
- Validation: rtk cargo test --offline --locked -p davinci-agent -p davinci-coding-agent passed 1,906 tests, 5 ignored across five suites (469 agent and 1,437 product). Clippy then found format-collect in the new host fixture; after direct string appends replaced it, the final product f03_host_ selection passed 8 tests, 1,434 filtered across two suites. Agent/product all-target offline locked Clippy with warnings denied, workspace formatting and scoped diff checks passed. Independent final review found no material findings and passed all four snapshot tests and the host tests. No live provider calls, fresh executable migration fixture, release build or installation was performed.
- Remaining next steps: explicit record-less legacy recovery/compatibility without fabricated authority; fresh-process migration acceptance; pre-prompt core/session switching activation; coordinator worker transport/fencing; live board and plan linkage. Snapshot capacity/compaction, branch isolation, missing historical observer detail, Unix behavior and the rest of all fourteen plans remain open. F01/F03 remain in progress, F02/F04–F14 not started, and verified totals remain 0/74 tasks and 0/75 requirements. No push/publish/install occurred; unrelated instruction edits remain outside checkpoints.

## Partial F03 activation before the first prompt

- Base revision: f604254. Shared runtime/session.rs now owns cold session recovery, durable journal activation and the session log observer. The product host delegates cold restoration and still owns turn observers/workflow execution. Agent::load_from_session activates this state before replacing messages, consent and session binding. A same-source live durable reload retains the existing lease, bus and worker handles; a cold load reconciles orphaned Running tasks. This supersedes the earlier explicit-reload binding invalidation described above.
- Production startup, both interactive new-session actions, RPC new_session and SDK creation use the shared load path. Existing switch/fork/import callers inherit activation. Sessionless startup and nested workers remain sessionless. Failed target recovery leaves the active agent/session intact. RuntimeLogSubscriber remains available through its prior public product-host path.
- RED: f03_session_load_activates_durable_tasks_before_prompt failed at the absent tasks.jsonl assertion. GREEN covers immediate journal creation, retained task/run on reload and cold resume, and exactly one TaskCreated observer entry. Additional tests cover a live claim surviving reload, competing writer rejection, orphan recovery after all live handles drop, and corrupt target journal rejection without switching or rewriting it. SDK/RPC tests assert activation before prompts. The SDK cold-resume fixture now drops its original owner before opening another writer.
- Validation: rtk cargo test --offline --locked -p davinci-agent -p davinci-coding-agent -- --test-threads=1 passed 1,909 tests, 5 ignored across five suites (472 agent, 1,437 product). An earlier concurrent run had the existing global-environment OAuth fixture fail; its focused retry and serialized full suite passed. Agent/product all-target offline locked Clippy with warnings denied, cargo fmt --check and diff checks passed. An initial process-fixture invocation selected zero tests due to shell quoting/filter mistakes; the corrected exact invocation selected one test and passed.
- Independent review found that --session-id changed only the in-memory ID and that path aliases could miss live reuse. Both regressions demonstrated the intended failure. Custom-ID creation now uses the existing JsonlSessionRepo constructor and reopens the persisted header before activation. Runtime binding/reuse and both sidecar paths use the canonical source. The custom-ID and alias reload tests passed independently in review; no further critical or important findings remained. After these fixes, the final serialized offline locked agent/product suites passed 1,911 tests, 5 ignored across five suites (473 agent, 1,438 product). Final all-target Clippy with warnings denied, formatting and diff checks passed.
- Final fresh debug build passed. target/debug/davinci.exe SHA-256: 793f5dc8fd91cff5e5b58614c485d517cc379252e39788a3a771e16aa38edc1b (supersedes the earlier build for this slice). The ignored tests::f03_print_process_resumes_task_journal fixture passed against that exact executable. Isolated offline RPC get_state process checks proved custom-ID creation before any prompt, exit/reopen by path and by ID with unchanged journal bytes, and --no-session returning no session file. This is Windows process/adapter proof, not live TUI, Unix, release or installed-binary acceptance. No provider calls or installation occurred.
- Remaining: record-less legacy compatibility, fresh-process migration acceptance, worker coordinator transport/fencing, live board/plan linkage, branch isolation, snapshot capacity/compaction and all other outstanding expansion/integration/platform/release gates. F01/F03 remain in progress and F02/F04–F14 not started; whole-task and requirement verification totals remain 0/74 and 0/75. RTK used throughout; Headroom stats still report an unreachable proxy and zero measured savings.
- Next worker integration audit: SubagentRequest has runtime_agent_id but no coordinator handle; run_nested_subagent creates a sessionless child whose prompt receives a fresh isolated registry. Graph workers are sessionless and task tools are absent from default roles, but writerExtraTools can introduce them. Route in-process worker commands to the parent-owned coordinator with host-derived identity and fence subprocess task mutation until its transport exists. Tests belong in subagent.rs and graph worker_hooks/roles; preserve normal root task tools. WorkflowExecutor direct task mutations and the missing task_get known-tool classification also need owning-plan integration. These are executable prerequisites, not external blockers.

## Partial F03 in-process worker coordinator binding

- Base revision: 56224e0. SubagentRequest now carries a host-only RuntimeHandle. RuntimeHandle::for_worker requires a registered Running child under the active parent/run, derives child identity and cancellation, and retains the parent's task registry, agent registry and mailbox. Workflow requests use the same binding and cancellation token. Sessionless nested prompts preserve that binding while keeping their own turn bus and observers; parent task observers and durable writer lease remain attached to the shared registry. Requests without a coordinator cannot expose task tools.
- The durable subagent regression creates a task through the child context, observes it in the parent, and reopens the session to verify persistence. It rejects unknown children and foreign parent/run bindings and proves child cancellation does not cancel the parent. The product host regression runs an authorized task command through complete_prompt_with_host and verifies shared task/run identity without creating a child session.
- An additional actual run_nested_subagent invocation initially expected another mutation and failed. Inspection confirmed the existing Edits permission policy asks for task mutation approval, while nested workers have no approver. The regression now asserts that sharing the coordinator preserves that denial and rejects an unbound task worker. No permission policy was widened; this is not proof of successful task mutation through the default nested-worker entry point.
- Validation: serialized offline locked agent/product suites passed 1,913 tests, 5 ignored across five suites before the final test extensions and shared workflow token adjustment. After those changes, the durable subagent test passed 1, product host/nested-boundary test passed 1, and workflow tests passed 25. Final agent/product all-target offline locked Clippy with warnings denied and diff checks passed; cargo fmt --all ran. No new executable build, process fixture, live provider, release or installation was performed for this slice.
- Final independent review found no material findings and independently passed the subagent coordinator test, product host/nested-boundary test and 25 workflow tests. Final cargo fmt --all --check passed.
- Remaining next inputs: graph subprocess task-tool fencing/transport, explicit worker mutation authorization through the owning decision contract, background-worker and stale-session fencing, workflow direct-command authority and task_get classification, record-less migration, branch isolation, journal compaction, live task board and all remaining expansion gates. F01/F03 remain in progress; F02/F04-F14 not started; no whole task or requirement is verified (0/74 and 0/75). This slice introduces no external blocker.

## Partial F03 graph subprocess coordinator fence

- Base revision: a156a54. Confirmed graph workers are sessionless subprocesses with no parent task coordinator transport. Writer extras previously advertised and admitted task_create/task_update/task_list/task_get into an unrelated local runtime. Controller worker specifications now omit these four tools. GraphWorkerContext filters them from extras and rejects them at pre-tool dispatch even if its allowlist is widened. Other writer extras and ordinary session tools retain their existing behavior. This temporary unsupported-operation fence is not the required transport implementation or completed F03.
- RED: both worker-hook and controller dispatch regressions failed with advertised task_create before implementation. GREEN: the two tests passed across lib/bin targets (4 tests); the full graph selection passed 312 tests across two suites. Tests cover all four tools, forced allowlist widening and retained custom writer extras.
- Fresh offline locked debug build passed. target/debug/davinci.exe SHA-256 b9aaf549f253b4864f600912fc4b6386aa6100e383d0ab84412cbbd04fb0598c. The exact ignored tests::f03_graph_process_rejects_unbound_task_tools fixture passed 1 test, launching four isolated offline graph-style processes with teams enabled, Always Approve and explicit task allowlists. Each produced a task tool error requiring parent coordinator transport. The exact ignored tests::f03_print_process_resumes_task_journal fixture passed 1 test against the same executable, preserving ordinary session task creation and cold resume. No installed release or live provider call was involved.
- Product all-target offline locked Clippy with warnings denied, cargo fmt --all --check and diff checks passed. RTK used throughout. Headroom stats report an unreachable proxy and zero measured savings.
- Independent security/correctness review found no material findings and independently passed the controller and hook regressions in both targets plus the explicit subprocess fixture against the debug build.
- Follow-up audit corrects the earlier task_get classification concern: workflow validation normally uses RuntimeCapabilityRegistry::with_builtins, and teams-enabled builtin_capabilities already contains read-only task_get. The static fallback omission alone does not prove an enabled-path bug; no speculative change was made.
- Next: implement authenticated parent coordinator transport and worker mutation authorization, then background/stale-session fencing and workflow direct-command authority. Record-less migration, branch isolation, compaction, task board and cross-feature gates remain open. F01/F03 in progress; F02/F04-F14 not started; 0/74 whole tasks and 0/75 requirements verified. No external blocker, push or installation.

## Partial F03 worker task admission lifetime

- Base revision: 380077f. All four task tools now validate worker contexts against the parent registry before command admission: the child must still be Running under the same parent and run, with neither its cancellation token nor tool context aborted. Root task contexts retain existing behavior. This is an admission check, not atomic cancellation of an already admitted command or graph subprocess transport.
- RED: a retained context created another task after its oneshot worker completed. GREEN: all four task tools reject that context, the parent snapshot remains unchanged, and cold session reopen preserves it. An initial durable assertion read the exclusively leased Windows journal and failed with sharing violation 32; the fixture now drops live owners and checks cold recovery. No journal lease behavior changed.
- Extended coordinator tests reject foreign run/parent identities and cancelled child contexts while preserving the parent token. A background/teammate fixture verifies task creation succeeds while each worker runs and retained task access is rejected after it settles. The product host fixture now registers its child identity, matching production lifecycle admission.
- Final serialized offline locked davinci-agent suite passed 476 tests across two suites. The product sessionless host test passed 1 test; workflow selection passed 25 tests. Agent/product all-target offline locked Clippy with warnings denied and workspace formatting check passed. Independent review found no material issues and independently passed the background lifetime test and earlier coordinator/host regressions. No fresh binary build, process fixture, release or installation was performed for this checkpoint. RTK used throughout; Headroom remains unavailable with zero measured savings.
- Next transport inputs: graph workers have piped output and null stdin, only DAVINCI_AGENT_ID propagation, and no coordinator channel or captured parent permission policy. Implement a graph-private bounded authenticated command channel with host-derived child identity, explicit task-tool allowlisting and parent policy; close it on completion, cancellation, timeout and shutdown. Keep the subprocess task fence until transport and authorization exist. In-flight command fencing belongs to the shared task/control commit boundary and must not hold a registry lock across completion hooks.
- Remaining: actual graph transport, explicit worker mutation authorization, stale-session/in-flight fencing, workflow direct-command authority, record-less migration, branch isolation, journal compaction, live task board and all expansion integration/platform/release gates. F01/F03 remain in progress; F02/F04-F14 not started; 0/74 whole tasks and 0/75 requirements verified. No external blocker, push or installation.

## Partial F03 parent-owned worker task transport

- Base revision: 7329504. Added parent-owned, per-worker authenticated loopback task transport in `runtime/task_transport.rs`. `TaskCoordinatorTransport::bind` derives worker identity through `RuntimeHandle::for_worker`, sharing the parent task registry and durable journal writer. It accepts only explicitly allowed task tools (`task_create`, `task_update`, `task_get`, `task_list`) and checks the parent's shared `PermissionState` for each request. Ask verdicts currently reject with "worker task command requires parent approval" pending interactive approval integration.
- Bounded JSON framing (8 MiB cap, bounded nonblocking I/O with deadlines, redacted client Debug, no automatic retries). Drop stops admission and joins the serving thread. Admitted commands and synchronous completion hooks retain their existing behavior.
- `apply_graph_session_context` in `main.rs` now attaches `agent.runtime_for_session().cloned()` to `GraphController`. A product regression covers initial binding and session replacement.
- IMPORTANT: Transport is NOT connected to graph subprocess task calls yet; the existing graph task-tool fence remains intentionally active.
- RED/GREEN: five transport unit tests in `task_transport.rs` (durable parent commit/reopen and policy/lifecycle checks, malformed requests/credentials/allowlists, partial-frame deadlines, maximum escaped task-list page, competing worker claims with exactly one winner and idempotent replay) and one product host regression in `main.rs` (`f03_graph_session_context_binds_current_parent_runtime`).
- Validation: `rtk cargo test -p davinci-agent -p davinci-coding-agent --offline --locked -- --test-threads=1` passed 1,925 tests, 6 ignored across five suites (481 agent, 1,444 product). Scoped rustfmt check and `rtk cargo clippy -p davinci-agent -p davinci-coding-agent --all-targets --offline --locked -- -D warnings` passed with no warnings. Scoped diff check verified.
- Next implementation: Connect graph subprocess task calls to the parent coordinator with the actual parent runtime, current permission authority, trusted approval/guard path, explicit role/tool allowlist, and actual worker abort/cancellation signal.

## Partial F03 graph worker coordinator connection and writer authorization

- Base revision: `cbb6488`. Connected graph subprocess worker execution to parent `TaskCoordinatorTransport` over loopback TCP with host-derived child identity, parent permission evaluation, and writer-only tool authorization.
- `TaskCoordinatorClient` implemented in `davinci-agent/src/runtime/task_transport.rs` with `{new, address, credential, from_env, call, call_with_abort}` and wire framing. Made `is_task_tool` `pub`. Transport bind now mirrors `worker.bus = parent.bus.clone()` so runtime permission and task events publish directly to parent subscribers.
- In `davinci-agent/src/tools.rs`, added `pub task_coordinator: Option<TaskCoordinatorClient>` to `ToolContext`. Permitted task tools in `execute_tool_with` when `team_tools_enabled() || context.task_coordinator.is_some()`.
- In `davinci-agent/src/runtime/tools_task.rs`, `task_create_tool`, `task_update_tool`, `task_get_tool`, `task_list_tool` delegate through `coordinator.call_with_abort(...)` when `context.task_coordinator.is_some()`.
- In `davinci-coding-agent/src/native_extensions/graph`:
  - `types.rs`: added `coordinator_client: Option<TaskCoordinatorClient>` to `WorkerSpec`.
  - `worker.rs`: in `run_worker`, propagates `DAVINCI_TASK_COORDINATOR_ADDR`, `DAVINCI_TASK_COORDINATOR_CREDENTIAL`, and `DAVINCI_EXPERIMENTAL_AGENT_TEAMS=1` to the worker child process environment when coordinator client is attached.
  - `mod.rs`: added `permissions: Option<Arc<davinci_agent::PermissionState>>` with `set_permissions` and `with_permissions` on `GraphController`.
  - `controller.rs`: added `permissions: Option<Arc<PermissionState>>` to `ControllerDeps`. In `worker_spec`, advertises task tools solely to `Role::Writer` and only when coordinator authority (`deps.runtime.is_some() && deps.permissions.is_some()`) is available. In `execute_node`, binds `TaskCoordinatorTransport` when writer specifies task tools and runtime/permissions are present, attaching the client to `WorkerSpec`. Drops transport cleanly when worker attempt finishes.
  - `worker_hooks.rs`: added `has_coordinator: bool` to `GraphWorkerContext`, detecting `DAVINCI_TASK_COORDINATOR_ADDR` in `from_env()`. In `from_parts_with_coordinator` and `block_reason`, allows task tools for `Role::Writer` when coordinator is present; non-writer roles remain strictly blocked.
- In `davinci-coding-agent/src/main.rs`:
  - `apply_graph_session_context` attaches `native.graph.set_permissions(Some(agent.permissions.clone()))`.
  - `build_agent` attaches `TaskCoordinatorClient::from_env()` to `agent.tool_context.task_coordinator`.
- RED/GREEN:
  - Added unit test `f03_task_coordinator_client_roundtrip_with_transport` verifying client communication, credential authentication, tool delegation, and parent bus publication.
  - Added test `f03_graph_worker_with_coordinator_allows_writer_task_tools` verifying writer role with coordinator allows task tools while non-writer roles reject them.
  - Added integration test `f03_graph_controller_binds_task_coordinator_for_writer_with_runtime_and_permissions` verifying writer receives coordinator client, calls `task_create` over TCP loopback, and parent task registry persists the task.
- Validation:
  - `rtk cargo test -p davinci-agent -p davinci-coding-agent --offline --locked -- --test-threads=1`: 1,930 passed, 6 ignored (5 suites, 234.43s).
  - `rtk cargo clippy -p davinci-agent -p davinci-coding-agent --all-targets --offline --locked -- -D warnings`: passed with zero warnings.
  - `rtk cargo fmt --all -- --check`: passed.
- Remaining F03 scope: workflow direct-command authority & known-tool classification; in-flight & stale-session fencing; record-less legacy recovery & fresh-process migration; live task board & plan linkage (F03-T5); branch isolation.

## Partial F03 live task board and surface projection

- Base revision: `1cd3637`. Completed F03 Task 5 live task board projection, surface adapters, plan linkage non-completion, and `/tasks` interactive integration.
- TUI Model & Projection (`crates/davinci-tui/src/davinci/model.rs`): Added `Screen::TaskBoard` to `Screen` enum. Defined `TaskBoardRow` and `TaskBoardSheet` with metadata (`id`, `title`, `status`, `state`, `owner`, `dependencies`, `parent_plan_step`, `evidence_refs`, `blocked_reasons`, `activity`, `updated_at_ms`). Wired `sheet_anchor` and `mode` ("opera") in `Model`.
- TUI View (`crates/davinci-tui/src/davinci/views/task_board.rs`): Implemented `render_task_board` rendering state glyphs, IDs, statuses, owners, activities, dependencies, blocked reasons, evidence refs, and plan step refs; implemented chrome with summary stats and sheet navigation hints; added inline unit tests for empty, populated, and chrome metadata.
- Surface Adapters & Scenarios (`crates/davinci-coding-agent/src/davinci_surfaces.rs`): Implemented `task_visible` branch/root filter, `task_state_to_tui`, `task_board_from_records`, and `task_board(runtime)`. Implemented and verified all 8 required F03-T5 test scenarios:
  1. `f03_resume_lineage`: verifies task records retain history, dependencies, and evidence across resume.
  2. `f03_resume_after_main_runtime_gets_a_new_run_id`: verifies runtime retains access to root session tasks when run ID changes.
  3. `f03_old_running_worker_orphaned`: verifies running worker tasks are reconciled to Failed on crash/recovery.
  4. `f03_no_tasks`: verifies empty task board renders empty notice cleanly.
  5. `f03_thousands_of_tasks_paginated`: verifies thousands of tasks are handled and visible in board projection.
  6. `f03_terminal_narrow`: verifies narrow terminal widths do not panic and render formatted rows.
  7. `f03_branch_specific_tasks_not_leaked`: verifies tasks from other execution branches are isolated.
  8. `f03_acceptance_of_plan_does_not_complete_tasks`: verifies plan acceptance does not falsely mark tasks as completed.
- Slash Command & Interactive Integration (`slash.rs`, `davinci_interactive.rs`, `main.rs`): Registered `/tasks` ("Execution checklist and live task board") with `SlashAction::Tasks`. Wired interactive sheet display via `open_task_board_sheet` and non-interactive/print fallback in `main.rs`.
- Workflow Validation (`runtime/workflow/validate.rs`): Added `"task_get"` to `KNOWN_TOOLS` and updated `is_mutating_tool_with_registry` to inspect dynamic registry and `KNOWN_TOOLS`, defaulting unknown tools to mutating to preserve read-lane concurrency for `task_get`.
- Tool Context Fencing (`runtime/tools_task.rs`): Preserved `"worker task authority is no longer active"` error message invariant on aborted/cancelled worker contexts.
- Validation:
  - `rtk cargo fmt --all -- --check`: passed (zero formatting diffs).
  - `rtk cargo clippy -p davinci-tui -p davinci-agent -p davinci-coding-agent --all-targets --offline --locked -- -D warnings`: passed with zero warnings.
  - `rtk cargo test -p davinci-tui -p davinci-agent -p davinci-coding-agent --offline --locked -- --test-threads=1`: passed 2,521 tests, 7 ignored across 7 test suites (238.81s).
- Remaining F03 scope: record-less legacy migration recovery without fabricated authority, and cross-feature integration with F01/F02/F06.

## F01 permission approval modal contract verification

- Base revision: `b379d88`. Completed F01 contract function and test suite parity across all five tasks and requirements F01-1 through F01-5.
- Task 1 (`approval.rs`): `f01_legal_scope` verifies policy-owned choices via `offer_scopes`, restricting persistent grants to exact representable ordinary files and single routine local commands.
- Task 2 (`approval.rs`): Implemented `reply_matches` validating challenge identity, argument digest, policy revision, and expiry deadline. Added contract test `f01_stale_reply` verifying digest, revision, and timestamp mismatch fail closed.
- Task 3 (`approval_modal.rs`, `views/mod.rs`): Created `approval_modal.rs` implementing `approval_key` input trapping contract (Esc -> deny, Enter -> confirm, Up/Down -> navigation, 1-5 -> number focus, others -> consume when open, delegate when closed) and `lines(model)` delegating to `views::ask::lines`. Added contract tests `f01_modal_traps_mode_key` and `f01_modal_key_navigation_mappings`. Registered module in `views/mod.rs`.
- Task 4 (`approval.rs`): Implemented `grant_applies` validating session identity match, canonical subject match, and absence of explicit denial. Added contract test `f01_grant_binding` verifying matching grant, cross-session rejection, and explicit denial override.
- Task 5 (`rpc.rs`): Implemented `approval_host_result` in `crates/davinci-coding-agent/src/rpc.rs` and contract test `f01_noninteractive_fail_closed_contract` verifying interactive/disconnected/resolved states.
- Validation:
  - `rtk cargo fmt --all -- --check`: passed (zero formatting diffs).
  - `rtk cargo clippy -p davinci-tui -p davinci-agent -p davinci-coding-agent --all-targets --offline --locked -- -D warnings`: passed with zero warnings.
  - `rtk cargo test -p davinci-tui -p davinci-agent -p davinci-coding-agent --offline --locked -- --test-threads=1`: passed 2,526 tests, 7 ignored across 7 suites (211.34s).
- Wave 1 completion & Wave 2 launch: Wave 1 core deliverables (F03 execution tasks & live task board and F01 permission approval modal contracts) are complete and verified. Next milestone advances to Wave 2, beginning with F05 Task 1 (contract schema and accepted-plan compilation).

## F05 Task 1 task-scoped execution contracts and plan compilation

- Base revision: `22bb651`. Completed F05 Task 1 contract schema, canonical digest, path scope enforcement, and accepted-plan compilation.
- TaskContract Schema & Canonical Digest (`crates/davinci-agent/src/runtime/contracts.rs`): Defined `TaskContract` with `id`, `revision`, `task_id`, `accepted_plan_revision`, `writable_paths`, `protected_paths`, `allowed_new_dependencies`, `external_effects`, `verification_requirements`, `artifact_write_roots`, and `digest`. Built deterministic SHA-256 canonical digest computation with sorted sets to guarantee auditability and race safety.
- Path Scope Normalization & Protection Invariant:
  - Implemented `normalize_relative_path` enforcing strict relative path semantics, forward slashes, rejecting absolute/UNC/drive-relative paths, parent traversal escapes (`..`), and Windows Alternate Data Streams (`:`).
  - Implemented `path_scope_allows` and `path_scope_allows_case` enforcing the strict core invariant: **Protected scope strictly wins**, regardless of list ordering or wider writable scopes.
  - Implemented `resolve_contract_path` with symlink/reparse point escape detection via `is_symlink_escape` and canonical relative path resolution against workspace root.
- Dependency Addition Semantic Verification:
  - Implemented `check_dependency_addition` enforcing `allowed_new_dependencies: false` policy.
  - Semantically inspects `Cargo.toml` (`[dependencies]`, `[dev-dependencies]`, etc.), `Cargo.lock` (`[[package]]` entries), and `package.json`. Strictly rejects sneaky lockfile-only or manifest additions while permitting non-dependency edits (e.g. version bumps).
- Plan Acceptance Contract Compilation (`crates/davinci-agent/src/planning.rs`):
  - Implemented `compile_plan_contract` compiling user-accepted `LivingPlan` steps into an immutable `TaskContract` snapshot with baseline protected scopes (`.git/`, `.davinci/`, `.pi/`, `.env`), plan revision binding, and verification requirements.
  - Wired contract installation into `/plan accept [mode]` and `/plan approve`. Enforced automatic invalidation (`clear_active_contract`) when plans are edited or rejected.
  - Exposed thread-safe `active_contract` on `ToolContext` and `Agent` accessors.
- Task Registry Integration (`crates/davinci-agent/src/runtime/tasks.rs`, `task_store.rs`):
  - Added `contract_digest: Option<String>` to `TaskRecord`, `TaskCreateRequest`, and receipt verification with backwards-compatible serde defaults and length bounds.
- Verification & Test Coverage:
  - `crates/davinci-agent/src/runtime/contracts.rs`: 11 targeted unit tests covering `f05_protected_wins`, `f05_path_prefix_collision`, `f05_protected_wins_even_if_later_writable_entry_matches`, `f05_normalize_relative_path_safety`, `f05_windows_ads`, `f05_same_file_via_alias`, `f05_symlink_reparse_escape`, `f05_case_sensitive_vs_insensitive_volume`, `f05_manifest_and_lockfile_change`, `f05_contract_digest_and_validation`, and `f05_compile_plan_contract_and_expansion`.
  - `crates/davinci-agent/src/planning.rs`: `plan_acceptance_compiles_task_contract` verifying end-to-end plan acceptance contract compilation and invalidation on rejection.
  - `cargo fmt --all -- --check`: passed (zero formatting diffs).
  - `cargo clippy -p davinci-tui -p davinci-agent -p davinci-coding-agent --all-targets --offline --locked -- -D warnings`: passed with zero warnings.
  - Full test suite passed across `davinci-agent`, `davinci-tui`, and `davinci-coding-agent`.
